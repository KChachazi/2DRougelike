# V1.5-3：程序化关卡生成 —— Seed 地图、实体房间与基本单局

> V1 第 5 周已经通过 `RoomConfig + Room + Door + LevelManager` 跑通固定三房：清空房间、碰门、传送到下一间。V1.5-3 不重新发明战斗系统，而是替换这条固定关卡生产方式。
>
> 本次更新的核心目标很明确：补齐单局事件语言，用 Seed 生成一张连通地图，把地图实例化为真实房间和通道，再复用现有刷怪、清房与死亡逻辑完成一局游戏。玩家死亡是失败，Boss 房清空是胜利，两种结果都进入同一结算与重开流程。
>
> 文档仍保留 8 个步骤和未来完成态 Reference，但需要区分范围：**步骤 1～4 构成本次必须完成的基本主流程；步骤 5～8 的混合相机、特殊房、局内强化和二维小地图属于后续深化，不阻塞本次核心验收。**每一步仍应保持可编译、可独立 Play 验收。

---

## 本次更新的核心目标

本次真正需要跑通的流程是：

```text
开始新局
   │
   ▼
RunManager 确定 Seed，并发布 RunStartedEvent
   │
   ▼
生成纯数据房间图
   │
   ▼
实例化房间、墙体、门与实体通道
   │
   ▼
Start 房 ─ Normal 房 ─ 分岔/转折 ─ Normal 房 ─ Boss 房
                        │
                        └────────── Normal 房
   │
   ├─ 首次进入战斗房：封门 → 生成敌人 → 清空 → 开门
   └─ 返回已清房间：不重复刷怪、不再次锁门
                              │
               ┌──────────────┴──────────────┐
               ▼                             ▼
            玩家死亡                    Boss 房清空
               │                             │
               └────── RunEndedEvent ───────┘
                              │
                    统一结算与重新开始
```

它包含四个最小闭环：

- **事件闭环**：Run、Level、Room 和 Enemy 的关键时间节点能够被统计、UI 和关卡流程理解。
- **结构闭环**：同一 Seed 生成同一张连通且不重叠的地图，地图至少包含 Start、Normal 和 Boss。
- **空间闭环**：数据图中的连接会生成真实通道；没有邻居的方向保持墙体封闭，玩家不再通过传送切房。
- **玩法闭环**：现有敌人生成和清房逻辑能够在随机房间中继续工作，并最终进入胜利或失败结算。

### 核心步骤与后续扩展

| 步骤 | 内容 | 本次是否阻塞验收 |
|---|---|---|
| 1 | Run 胜负、Seed、统计与重开事件 | 是，已完成 |
| 2 | 生成并验证纯数据地图 | 是 |
| 3 | 实例化房间与实体通道 | 是 |
| 4 | 接回封门、刷怪、清房、重访与 Boss 胜利 | 是 |
| 5 | 房间固定镜头与通道跟随镜头 | 否，后续深化 |
| 6 | Elite、Treasure、Recovery 等特殊房行为 | 否，后续深化 |
| 7 | 三选一局内强化 | 否，后续深化 |
| 8 | 二维拓扑小地图与探索迷雾 | 否，后续深化 |

后半部分代码仍按未来完整形态编写，因此步骤 2～4 的 Reference 可能提前包含特殊房标签、奖励等待接口或更严格的生成约束。它们可以保留，方便以后继续深化；但在当前基本单局阶段，**本节定义的核心验收范围优先**，不能因为尚未实现奖励、特殊房或二维小地图而阻塞地图生成主流程。

---

## 0. 先冻结本次范围

### 0.1 这次必须完成什么

- 更新或适配全局事件，使 `RunStarted`、`LevelStarted`、`RoomEntered`、`RoomCleared`、`RunEnded` 等关键节点拥有稳定语义。
- 根据 `RunManager.CurrentSeed` 生成房间数量、坐标、类型和四向邻居。
- 把纯数据地图实例化为房间与通道，并让玩家能够在真实空间中移动。
- 复用现有 `RoomConfig`、敌人生成、`Health.Died` 计数和清房逻辑。
- Boss 房清空时调用 `RunManager.CompleteRun()`；玩家死亡继续由 `RunManager` 结算失败。
- 同 Seed 和新 Seed 都能重新开始，且不残留上一局的房间与统计状态。

### 0.2 这次暂时不要求什么

- 不要求 Elite、Treasure、Recovery 三种房型现在就拥有独立玩法；枚举和生成标签可以保留，但当前流程只依赖 Start、Normal、Boss 的行为。
- 不要求局内强化、奖励池、稀有度或构筑系统。
- 不要求把旧横排小地图立刻升级成二维拓扑和探索迷雾。
- 不要求房间固定镜头与通道跟随镜头同时完成；只要玩家能稳定观察和游玩生成地图即可。
- 不要求环路、锁匙门、商店、传送点或复杂房间模板组合。

这些内容在后续章节继续保留，等基本单局稳定后可以逐项加入，但不能反过来阻塞地图生成与实例化。

### 0.3 数据、实例和流程仍然分层

实现虽然收敛，职责仍应拆开：

| 层 | 负责什么 | 不负责什么 |
|---|---|---|
| `LevelGraph` | 房间 Id、网格坐标、深度、类型、邻居 | 不实例化 GameObject，不生成敌人 |
| `LevelGraphGenerator` | 根据 Seed 创建连通、不重叠的纯数据地图 | 不读取场景，不调用 `Instantiate` |
| `DungeonBuilder` | 把图变成房间和通道，连接 `RoomConfig` | 不决定何时封门、何时胜利 |
| `Room + LevelManager` | 进入、封门、清房、重访与 Boss 通关 | 不发明地图拓扑 |

纯数据层可以连续验证大量 Seed；空间预制体以后从灰盒替换为 Tilemap 时，也不必重写生成算法。

### 0.4 全局事件是通知语言，不是总流程控制器

全局 EventBus 适合通知跨模块事实：

- `RunStartedEvent` / `RunEndedEvent`：供结算、统计和 UI 使用；
- `LevelStartedEvent`：携带与运行时对象解耦的地图快照；
- `RoomEnteredEvent` / `RoomClearedEvent`：供 Run 统计和小地图观察；
- `EnemyDiedEvent`：供击杀统计与战斗反馈使用。

但同一房间内部仍然使用直接引用和局部 C# 事件：`Room` 订阅敌人 `Health.Died`，`DungeonBuilder` 把生成结果直接交给 `LevelManager`。不要为了“事件化”而把一次简单函数调用拆成多层广播。

### 0.5 首版地图规则

核心生成器使用二维网格上的树：每个新房间只连接一个已有房间，因此边数恒为 `房间数 - 1`。首版只要求：

- 房间坐标不重叠；
- 全部房间从 Start 可达；
- 0 号房为 Start，未做特殊分配的房间默认为 Normal；
- 距离 Start 最远的房间为 Boss；
- 同一 Seed 生成相同的房间数量、坐标、连接、类型和 `RoomConfig` 选择。

环路、特殊房数量、Boss 最小深度和强制分岔都可以在基本流程稳定后再强化。完整 Reference 已实现其中一部分约束，照常使用也没有问题，但它们不是本轮基本流程成立的前提。拓扑随机与 `RoomConfig` 选择仍建议使用相互独立的 `System.Random`，避免未来增加一次内容抽取就改变整张地图。

---

## 步骤 1：先补齐一局游戏的胜负与重开闭环

**这一步做什么**：在开始重构地图前，先把“什么是一局”定义清楚。`RunManager` 决定 Seed、记录最小统计、监听玩家死亡，并统一发出胜负结算；`RunResultUI` 替换只能显示胜利的旧 `VictoryUI`。

这一步暂时不改固定三房流程，所以很适合先独立验收。

### 1.1 更新 `Assets/Scripts/Core/GameEvents.cs`

这是跨模块协议变更，后面的步骤也会持续使用。为了避免文档中前后出现多个过渡版本，下面直接给出 V1.5-3 的**最终完整文件**。

```csharp
using UnityEngine;

namespace Game.Core
{
    // ======================== 玩家 ========================
    /// <summary>玩家当前血量发生变化，供血条等跨模块表现刷新。</summary>
    public readonly struct PlayerHealthChangedEvent
    {
        public readonly int Current;
        public readonly int Max;
        public PlayerHealthChangedEvent(int current, int max) { Current = current; Max = max; }
    }
    // ======================== 武器 ========================
    /// <summary>当前武器的剩余弹药发生变化。</summary>
    public readonly struct AmmoChangedEvent
    {
        public readonly int Current;
        /// <summary>最大弹药量；-1 表示无限弹药。</summary>
        public readonly int Max;
        public AmmoChangedEvent(int current, int max) { Current = current; Max = max; }
    }
    /// <summary>玩家切换了当前武器，携带用于 UI 显示的武器名称。</summary>
    public readonly struct WeaponChangedEvent
    {
        public readonly string WeaponName;
        public WeaponChangedEvent(string weaponName) { WeaponName = weaponName; }
    }
    // ======================== 技能 ========================
    /// <summary>跨模块识别技能的稳定标识。</summary>
    public enum SkillId
    {
        Dash,
        Grenade,
    }
    /// <summary>指定技能开始进入冷却，UI 可据此独立倒计时。</summary>
    public readonly struct SkillCooldownStartedEvent
    {
        public readonly SkillId Skill;
        public readonly float Cooldown;
        public SkillCooldownStartedEvent(SkillId skill, float cooldown) { Skill = skill; Cooldown = cooldown; }
    }
    // ======================== 一局游戏 ========================
    public enum RunResult
    {
        Victory,
        Defeat,
    }

    /// <summary>新的一局已经开始，并确定了本局 Seed。</summary>
    public readonly struct RunStartedEvent
    {
        public readonly int Seed;
        public RunStartedEvent(int seed) { Seed = seed; }
    }

    /// <summary>本局以胜利或失败结束，并携带最小结算统计。</summary>
    public readonly struct RunEndedEvent
    {
        public readonly RunResult Result;
        public readonly int Seed;
        public readonly int RoomsVisited;
        public readonly int EnemiesDefeated;
        public readonly int UpgradesCollected;

        public RunEndedEvent(
            RunResult result,
            int seed,
            int roomsVisited,
            int enemiesDefeated,
            int upgradesCollected)
        {
            Result = result;
            Seed = seed;
            RoomsVisited = roomsVisited;
            EnemiesDefeated = enemiesDefeated;
            UpgradesCollected = upgradesCollected;
        }
    }

    /// <summary>玩家在本局选择了一项强化。</summary>
    public readonly struct RunUpgradeSelectedEvent
    {
        public readonly string UpgradeName;
        public RunUpgradeSelectedEvent(string upgradeName) { UpgradeName = upgradeName; }
    }

    // ======================== 关卡 ========================
    /// <summary>房间的玩法类型，用于生成规则、内容配置和小地图显示。</summary>
    public enum RoomType
    {
        Start,
        Normal,
        Elite,
        Treasure,
        Recovery,
        Boss,
    }

    /// <summary>网格地图中的四个稳定方向，枚举值同时作为邻居数组索引。</summary>
    public enum RoomDirection
    {
        North = 0,
        East = 1,
        South = 2,
        West = 3,
    }

    /// <summary>给 UI 使用的只读房间快照，不暴露 Level 模块的运行时对象。</summary>
    public readonly struct RoomMapData
    {
        public readonly int Id;
        public readonly Vector2Int GridPosition;
        public readonly RoomType Type;
        public readonly int NorthId;
        public readonly int EastId;
        public readonly int SouthId;
        public readonly int WestId;

        public RoomMapData(
            int id,
            Vector2Int gridPosition,
            RoomType type,
            int northId,
            int eastId,
            int southId,
            int westId)
        {
            Id = id;
            GridPosition = gridPosition;
            Type = type;
            NorthId = northId;
            EastId = eastId;
            SouthId = southId;
            WestId = westId;
        }

        public int GetNeighborId(RoomDirection direction)
        {
            switch (direction)
            {
                case RoomDirection.North: return NorthId;
                case RoomDirection.East: return EastId;
                case RoomDirection.South: return SouthId;
                case RoomDirection.West: return WestId;
                default: return -1;
            }
        }
    }

    /// <summary>关卡完成建造，并提供完整但与运行时对象解耦的地图快照。</summary>
    public readonly struct LevelStartedEvent
    {
        public readonly RoomMapData[] Rooms;
        public LevelStartedEvent(RoomMapData[] rooms) { Rooms = rooms; }
    }
    /// <summary>玩家进入指定 Id 的房间。</summary>
    public readonly struct RoomEnteredEvent
    {
        public readonly int RoomId;
        public RoomEnteredEvent(int roomId) { RoomId = roomId; }
    }
    /// <summary>指定 Id 的房间已经完成战斗或特殊房效果。</summary>
    public readonly struct RoomClearedEvent
    {
        public readonly int RoomId;
        public RoomClearedEvent(int roomId) { RoomId = roomId; }
    }
    /// <summary>当前关卡的全部房间流程已经完成。</summary>
    public readonly struct LevelCompletedEvent { }
    // ======================== 表现 ========================
    /// <summary>敌人受到一次伤害，供命中停顿和命中特效等表现系统使用。</summary>
    public readonly struct EnemyDamagedEvent
    {
        public readonly Vector2 Position;
        public readonly int Damage;
        public EnemyDamagedEvent(Vector2 position, int damage) { Position = position; Damage = damage; }
    }
    /// <summary>敌人死亡，携带死亡位置供击杀反馈使用。</summary>
    public readonly struct EnemyDiedEvent
    {
        public readonly Vector2 Position;
        public EnemyDiedEvent(Vector2 position) { Position = position; }
    }
    /// <summary>请求当前启用的相机按指定强度和时长执行震屏。</summary>
    public readonly struct ScreenShakeEvent
    {
        public readonly float Intensity;
        public readonly float Duration;
        public ScreenShakeEvent(float intensity, float duration) { Intensity = intensity; Duration = duration; }
    }
    // ======================== V1.5：状态异常反馈 ========================
    /// <summary>
    /// 实体被施加状态异常时广播。
    /// 例如：敌人头上冒出火焰图标、玩家屏幕边缘结冰等。
    /// 目前 StatusEffectManager 并未主动 Publish 此事件。
    /// </summary>
    public readonly struct StatusAppliedEvent
    {
        /// <summary>受影响的实体</summary>
        public readonly GameObject Target;
        /// <summary>被施加的状态类型</summary>
        public readonly StatusType Type;
        /// <summary>效果持续秒数</summary>
        public readonly float Duration;
        public StatusAppliedEvent(GameObject target, StatusType type, float duration)
        {
            Target = target;
            Type = type;
            Duration = duration;
        }
    }
    /// <summary>
    /// 状态异常过期或被移除时广播。
    /// 目前尚未由 StatusEffectManager 发布，属于后续表现系统的预留协议。
    /// </summary>
    public readonly struct StatusExpiredEvent
    {
        public readonly GameObject Target;
        public readonly StatusType Type;
        public StatusExpiredEvent(GameObject target, StatusType type)
        {
            Target = target;
            Type = type;
        }
    }
}
```

关键变化：

- 新增 `RunStartedEvent`、`RunEndedEvent` 和 `RunUpgradeSelectedEvent`；
- `RoomType` 扩展为六种；
- 房间事件从旧数组下标语义改成稳定的 `RoomId`；
- `LevelStartedEvent` 携带与运行时对象解耦的 `RoomMapData[]`；
- 删除旧 `DoorEnteredEvent`。实体地图中门不再负责“告诉 LevelManager 传送到下一间”。

在步骤 1 里，旧 `LevelManager` 还依赖 `DoorEnteredEvent`。因此**不要只抄 GameEvents 就停下**：步骤 1 只把下面这个完整单元追加到现有 `GameEvents.cs` 的命名空间内；等步骤 3 重写 `LevelManager` 时，再把整个文件同步成上面的最终版本。Reference 保存的是最终完成态。

```csharp
// ======================== 一局游戏 ========================
public enum RunResult
{
    Victory,
    Defeat,
}

public readonly struct RunStartedEvent
{
    public readonly int Seed;
    public RunStartedEvent(int seed) { Seed = seed; }
}

public readonly struct RunEndedEvent
{
    public readonly RunResult Result;
    public readonly int Seed;
    public readonly int RoomsVisited;
    public readonly int EnemiesDefeated;
    public readonly int UpgradesCollected;

    public RunEndedEvent(
        RunResult result,
        int seed,
        int roomsVisited,
        int enemiesDefeated,
        int upgradesCollected)
    {
        Result = result;
        Seed = seed;
        RoomsVisited = roomsVisited;
        EnemiesDefeated = enemiesDefeated;
        UpgradesCollected = upgradesCollected;
    }
}

public readonly struct RunUpgradeSelectedEvent
{
    public readonly string UpgradeName;
    public RunUpgradeSelectedEvent(string upgradeName) { UpgradeName = upgradeName; }
}
```

### 1.2 新建 `Assets/Scripts/Core/RunManager.cs`

```csharp
using System;
using System.Collections.Generic;
using Game.Commands;
using Game.Debug;
using Game.Entities;
using UnityEngine;
using UnityEngine.SceneManagement;

namespace Game.Core
{
    /// <summary>管理一局游戏的 Seed、最小统计、胜负结算与重新开始。</summary>
    [DefaultExecutionOrder(-500)]
    [DisallowMultipleComponent]
    public sealed class RunManager : MonoBehaviour
    {
        private static bool hasPendingSeed;
        private static int pendingSeed;

        public static RunManager Instance { get; private set; }

        [Header("Seed")]
        [SerializeField] private bool randomizeSeed = true;
        [SerializeField] private int fixedSeed = 12345;
        [SerializeField] private bool freezeTimeOnRunEnd = true;

        private readonly HashSet<int> visitedRooms = new HashSet<int>();
        private Health playerHealth;
        private bool ended;
        private int enemiesDefeated;
        private int upgradesCollected;

        public int CurrentSeed { get; private set; }
        public bool HasEnded => ended;

        [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]
        private static void ResetStatics()
        {
            Instance = null;
            hasPendingSeed = false;
            pendingSeed = 0;
        }

        private void Awake()
        {
            if (Instance != null && Instance != this)
            {
                Destroy(gameObject);
                return;
            }
            Instance = this;
            Time.timeScale = 1f;

            if (hasPendingSeed)
            {
                CurrentSeed = pendingSeed;
                hasPendingSeed = false;
            }
            else
            {
                CurrentSeed = randomizeSeed ? CreateRandomSeed() : fixedSeed;
            }
        }

        private void OnEnable()
        {
            EventBus.Subscribe<RoomEnteredEvent>(OnRoomEntered);
            EventBus.Subscribe<EnemyDiedEvent>(OnEnemyDied);
            EventBus.Subscribe<RunUpgradeSelectedEvent>(OnUpgradeSelected);
        }

        private void Start()
        {
            GameObject player = GameManager.Instance != null ? GameManager.Instance.Player : null;
            if (player != null && player.TryGetComponent(out playerHealth))
                playerHealth.Died += OnPlayerDied;
            else
                GameDebug.Error(DebugCategory.System, "RunManager 找不到玩家 Health，失败结算不会触发。", this);

            EventBus.Publish(new RunStartedEvent(CurrentSeed));
            GameDebug.Log(DebugCategory.Level, $"新一局开始，Seed={CurrentSeed}。", this);
        }

        private void OnDisable()
        {
            EventBus.Unsubscribe<RoomEnteredEvent>(OnRoomEntered);
            EventBus.Unsubscribe<EnemyDiedEvent>(OnEnemyDied);
            EventBus.Unsubscribe<RunUpgradeSelectedEvent>(OnUpgradeSelected);
            if (playerHealth != null) playerHealth.Died -= OnPlayerDied;
            if (Instance == this) Instance = null;
        }

        public void CompleteRun()
        {
            EndRun(RunResult.Victory);
        }

        public void RestartSameSeed()
        {
            hasPendingSeed = true;
            pendingSeed = CurrentSeed;
            ReloadCurrentScene();
        }

        public void RestartNewSeed()
        {
            hasPendingSeed = false;
            ReloadCurrentScene();
        }

        private void OnPlayerDied()
        {
            EndRun(RunResult.Defeat);
        }

        private void EndRun(RunResult result)
        {
            if (ended) return;
            ended = true;
            SetPlayerInputEnabled(false);

            if (result == RunResult.Victory)
                EventBus.Publish(new LevelCompletedEvent());

            EventBus.Publish(new RunEndedEvent(
                result,
                CurrentSeed,
                visitedRooms.Count,
                enemiesDefeated,
                upgradesCollected));

            GameDebug.Log(DebugCategory.System, $"本局结束：{result}，Seed={CurrentSeed}。", this);
            if (freezeTimeOnRunEnd) Time.timeScale = 0f;
        }

        private void OnRoomEntered(RoomEnteredEvent e) => visitedRooms.Add(e.RoomId);
        private void OnEnemyDied(EnemyDiedEvent e) => enemiesDefeated++;
        private void OnUpgradeSelected(RunUpgradeSelectedEvent e) => upgradesCollected++;

        private static int CreateRandomSeed()
        {
            return unchecked(Environment.TickCount * 397 ^ DateTime.UtcNow.Millisecond);
        }

        private static void SetPlayerInputEnabled(bool enabled)
        {
            GameObject player = GameManager.Instance != null ? GameManager.Instance.Player : null;
            if (player != null && player.TryGetComponent(out PlayerInputHandler input))
                input.enabled = enabled;
        }

        private static void ReloadCurrentScene()
        {
            Time.timeScale = 1f;
            SceneManager.LoadScene(SceneManager.GetActiveScene().buildIndex);
        }
    }
}
```

`[DefaultExecutionOrder(-500)]` 很重要：`LevelManager.Start()` 读取 Seed 之前，`RunManager.Awake()` 必须已经确定本局 Seed。

`pendingSeed` 是同场景重载之间的最小桥梁：

- “同 Seed 重开”先保存当前 Seed，再重新载入场景；
- “新 Seed 重开”不保存，下次 `Awake` 重新生成；
- 它不是存档。关闭程序后无需保留，正符合一局游戏的生命周期。

### 1.3 让当前固定 `LevelManager` 接入 RunManager

步骤 1 仍使用旧固定三房。把现有 `EnterRoom(int index)` 中越界通关的完整分支替换为：

```csharp
if (index < 0 || index >= rooms.Length)
{
    if (RunManager.Instance != null)
        RunManager.Instance.CompleteRun();
    else
        EventBus.Publish(new LevelCompletedEvent());

    GameDebug.Log(DebugCategory.Level, "恭喜通关！", this);
    return;
}
```

这样旧流程抵达数组末尾时也会走新的 Run 胜利结算。步骤 3 的最终 `LevelManager` 已在 Boss 清空时使用同一入口。

### 1.4 新建 `Assets/Scripts/UI/RunResultUI.cs`

```csharp
using Game.Core;
using TMPro;
using UnityEngine;

namespace Game.UI
{
    /// <summary>统一显示胜利或失败结算，并提供同 Seed/新 Seed 重开入口。</summary>
    public sealed class RunResultUI : MonoBehaviour
    {
        [SerializeField] private GameObject panel;
        [SerializeField] private TMP_Text titleText;
        [SerializeField] private TMP_Text summaryText;

        private void Awake()
        {
            if (panel != null) panel.SetActive(false);
        }

        private void OnEnable() => EventBus.Subscribe<RunEndedEvent>(OnRunEnded);
        private void OnDisable() => EventBus.Unsubscribe<RunEndedEvent>(OnRunEnded);

        public void RestartSameSeed()
        {
            if (RunManager.Instance != null) RunManager.Instance.RestartSameSeed();
        }

        public void RestartNewSeed()
        {
            if (RunManager.Instance != null) RunManager.Instance.RestartNewSeed();
        }

        private void OnRunEnded(RunEndedEvent e)
        {
            if (panel != null) panel.SetActive(true);
            if (titleText != null) titleText.text = e.Result == RunResult.Victory ? "通关成功" : "本局失败";
            if (summaryText != null)
            {
                summaryText.text =
                    $"Seed：{e.Seed}\n" +
                    $"探索房间：{e.RoomsVisited}\n" +
                    $"击败敌人：{e.EnemiesDefeated}\n" +
                    $"获得强化：{e.UpgradesCollected}";
            }
        }
    }
}
```

### 1.5 Unity 场景配置

1. 在 `GameManager` 物体上添加 `RunManager`。
2. 调试阶段取消 `Randomize Seed`，把 `Fixed Seed` 设为 `12345`；完成同 Seed 验证后再打开随机。
3. 在 Canvas 下新建 `RunResultPanel`，初始是否激活都可以，脚本 `Awake` 会隐藏它。
4. Panel 下放标题、统计文本和两个按钮：`同 Seed 重开`、`新 Seed 重开`。
5. 把 `RunResultUI` 挂到一个**始终激活**的 Canvas 根物体，不要挂在默认禁用的 Panel 本身，否则它收不到事件。
6. 两个按钮的 `OnClick` 分别连接 `RunResultUI.RestartSameSeed()` 和 `RestartNewSeed()`。
7. 暂时保留旧 `VictoryUI` 组件，但禁用它，避免胜利时出现两套面板。步骤 1 验收通过后删除场景中的旧组件，并删除 `Assets/Scripts/UI/VictoryUI.cs`。

### 1.6 暂时触发失败用于验收

不要在生产代码里加“按键自杀”。直接在 Play 时让敌人击杀玩家，或者临时把玩家生命设低。检查：

- 玩家死亡后显示“本局失败”，输入停止，时间冻结；
- 点击同 Seed 重开后场景恢复、`Time.timeScale` 回到 1；
- 两种重开都不会残留上局统计；
- 走完当前 Boss 房仍能显示胜利结算；
- Console/DebugOverlay 中能看到本局 Seed。

> ✅ **步骤 1 验收标准**：固定三房版本仍可从开始走到胜利；玩家死亡能失败结算；同 Seed 与新 Seed 两个按钮都能重新开始；旧 VictoryUI 已安全移除。

---

## 步骤 2：只生成“纯数据地图”，暂时不实例化房间

**这一步做什么**：建立二维网格图、生成规则和验证器，并用 Gizmos 与连续 Seed 验证确认算法可靠。此时场景中的固定三房完全不动。

### 2.1 新建 `Assets/Scripts/Level/Generation/RoomDirectionUtility.cs`

```csharp
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>提供房间方向与网格偏移之间的稳定转换。</summary>
    public static class RoomDirectionUtility
    {
        public static readonly RoomDirection[] All =
        {
            RoomDirection.North,
            RoomDirection.East,
            RoomDirection.South,
            RoomDirection.West,
        };

        public static Vector2Int ToOffset(RoomDirection direction)
        {
            switch (direction)
            {
                case RoomDirection.North: return Vector2Int.up;
                case RoomDirection.East: return Vector2Int.right;
                case RoomDirection.South: return Vector2Int.down;
                case RoomDirection.West: return Vector2Int.left;
                default: return Vector2Int.zero;
            }
        }

        public static RoomDirection Opposite(RoomDirection direction)
        {
            switch (direction)
            {
                case RoomDirection.North: return RoomDirection.South;
                case RoomDirection.East: return RoomDirection.West;
                case RoomDirection.South: return RoomDirection.North;
                case RoomDirection.West: return RoomDirection.East;
                default: return RoomDirection.North;
            }
        }
    }
}
```

### 2.2 新建 `Assets/Scripts/Level/Generation/RoomNode.cs`

```csharp
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>程序化关卡中的一个纯数据房间节点。</summary>
    public sealed class RoomNode
    {
        private readonly int[] neighborIds = { -1, -1, -1, -1 };

        public int Id { get; }
        public Vector2Int GridPosition { get; }
        public int Depth { get; }
        public RoomType Type { get; internal set; }

        public int Degree
        {
            get
            {
                int count = 0;
                for (int i = 0; i < neighborIds.Length; i++)
                    if (neighborIds[i] >= 0) count++;
                return count;
            }
        }

        public bool IsLeaf => Degree == 1;

        internal RoomNode(int id, Vector2Int gridPosition, int depth)
        {
            Id = id;
            GridPosition = gridPosition;
            Depth = depth;
            Type = RoomType.Normal;
        }

        public int GetNeighborId(RoomDirection direction) => neighborIds[(int)direction];

        internal void SetNeighbor(RoomDirection direction, int neighborId)
        {
            neighborIds[(int)direction] = neighborId;
        }

        public RoomMapData ToMapData()
        {
            return new RoomMapData(
                Id,
                GridPosition,
                Type,
                neighborIds[(int)RoomDirection.North],
                neighborIds[(int)RoomDirection.East],
                neighborIds[(int)RoomDirection.South],
                neighborIds[(int)RoomDirection.West]);
        }
    }
}
```

### 2.3 新建 `Assets/Scripts/Level/Generation/LevelGraph.cs`

```csharp
using System.Collections.Generic;
using System.Text;
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>保存一局关卡的节点、坐标与连接关系，不负责实例化场景对象。</summary>
    public sealed class LevelGraph
    {
        private readonly List<RoomNode> nodes = new List<RoomNode>();
        private readonly Dictionary<Vector2Int, RoomNode> nodesByPosition =
            new Dictionary<Vector2Int, RoomNode>();

        public IReadOnlyList<RoomNode> Nodes => nodes;
        public int Count => nodes.Count;
        public int StartId { get; internal set; } = -1;
        public int BossId { get; internal set; } = -1;

        internal RoomNode AddNode(Vector2Int position, int depth)
        {
            RoomNode node = new RoomNode(nodes.Count, position, depth);
            nodes.Add(node);
            nodesByPosition.Add(position, node);
            return node;
        }

        internal void Connect(RoomNode from, RoomDirection direction, RoomNode to)
        {
            from.SetNeighbor(direction, to.Id);
            to.SetNeighbor(RoomDirectionUtility.Opposite(direction), from.Id);
        }

        public RoomNode GetNode(int id)
        {
            return id >= 0 && id < nodes.Count ? nodes[id] : null;
        }

        public bool TryGetNodeAt(Vector2Int position, out RoomNode node)
        {
            return nodesByPosition.TryGetValue(position, out node);
        }

        public RoomMapData[] CreateMapSnapshot()
        {
            RoomMapData[] result = new RoomMapData[nodes.Count];
            for (int i = 0; i < nodes.Count; i++) result[i] = nodes[i].ToMapData();
            return result;
        }

        /// <summary>生成稳定签名，用于确认相同 Seed 的结果完全一致。</summary>
        public string BuildSignature()
        {
            StringBuilder builder = new StringBuilder(nodes.Count * 32);
            for (int i = 0; i < nodes.Count; i++)
            {
                RoomNode node = nodes[i];
                builder.Append(node.Id).Append('@')
                    .Append(node.GridPosition.x).Append(',').Append(node.GridPosition.y)
                    .Append(':').Append((int)node.Type).Append(':');

                for (int d = 0; d < RoomDirectionUtility.All.Length; d++)
                    builder.Append(node.GetNeighborId(RoomDirectionUtility.All[d])).Append(',');
                builder.Append('|');
            }
            return builder.ToString();
        }
    }
}
```

`BuildSignature()` 不是存档格式，它是确定性测试工具。同一个 Seed 连续生成两次，签名必须逐字符相同。

### 2.4 新建 `Assets/Scripts/Level/Generation/LevelGenerationSettings.cs`

```csharp
using UnityEngine;

namespace Game.Level
{
    /// <summary>程序化关卡的规模、特殊房数量与结构约束。</summary>
    [CreateAssetMenu(fileName = "LevelGenerationSettings", menuName = "Game/Level Generation Settings")]
    public sealed class LevelGenerationSettings : ScriptableObject
    {
        [Header("规模")]
        [SerializeField, Min(6)] private int minRoomCount = 9;
        [SerializeField, Min(6)] private int maxRoomCount = 12;
        [SerializeField, Min(1)] private int maxGenerationAttempts = 50;

        [Header("结构约束")]
        [SerializeField, Min(2)] private int minBossDepth = 4;
        [Tooltip("Degree >= 3 的房间至少有几个")]
        [SerializeField, Min(1)] private int minBranchRoomCount = 1;

        [Header("特殊房间")]
        [SerializeField, Min(0)] private int treasureRoomCount = 1;
        [SerializeField, Min(0)] private int recoveryRoomCount = 1;
        [SerializeField, Min(0)] private int eliteRoomCount = 1;
        [SerializeField, Min(1)] private int minEliteDepth = 2;

        public int MinRoomCount => minRoomCount;
        public int MaxRoomCount => Mathf.Max(minRoomCount, maxRoomCount);
        public int MaxGenerationAttempts => Mathf.Max(1, maxGenerationAttempts);
        public int MinBossDepth => Mathf.Max(1, minBossDepth);
        public int MinBranchRoomCount => Mathf.Max(0, minBranchRoomCount);
        public int TreasureRoomCount => Mathf.Max(0, treasureRoomCount);
        public int RecoveryRoomCount => Mathf.Max(0, recoveryRoomCount);
        public int EliteRoomCount => Mathf.Max(0, eliteRoomCount);
        public int MinEliteDepth => Mathf.Max(1, minEliteDepth);

        private void OnValidate()
        {
            minRoomCount = Mathf.Max(6, minRoomCount);
            maxRoomCount = Mathf.Max(minRoomCount, maxRoomCount);
            maxGenerationAttempts = Mathf.Max(1, maxGenerationAttempts);
        }
    }
}
```

推荐先使用默认值：

| 字段 | 初始值 | 意义 |
|---|---:|---|
| Min / Max Room Count | 9 / 12 | 一局规模仍适合快速验收 |
| Max Generation Attempts | 50 | 不满足特殊叶节点约束时自动重试 |
| Min Boss Depth | 4 | Boss 至少离起点四步 |
| Min Branch Room Count | 1 | 至少有一个度数 ≥ 3 的分岔房 |
| Treasure / Recovery / Elite | 1 / 1 / 1 | 每局都有三种特殊体验 |
| Min Elite Depth | 2 | 精英房不贴着出生点 |

### 2.5 新建 `Assets/Scripts/Level/Generation/LevelGraphGenerator.cs`

```csharp
using System;
using System.Collections.Generic;
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>使用独立 Seed 在二维网格上生成连通、无环且带分支的房间图。</summary>
    public static class LevelGraphGenerator
    {
        private readonly struct FrontierCandidate
        {
            public readonly int ParentId;
            public readonly RoomDirection Direction;
            public readonly Vector2Int Position;

            public FrontierCandidate(int parentId, RoomDirection direction, Vector2Int position)
            {
                ParentId = parentId;
                Direction = direction;
                Position = position;
            }
        }

        public static bool TryGenerate(
            LevelGenerationSettings settings,
            int seed,
            out LevelGraph graph,
            out string error)
        {
            if (settings == null)
            {
                graph = null;
                error = "没有指定 LevelGenerationSettings。";
                return false;
            }

            string lastError = "未知生成错误。";
            for (int attempt = 0; attempt < settings.MaxGenerationAttempts; attempt++)
            {
                int attemptSeed = unchecked(seed + attempt * 7919);
                System.Random random = new System.Random(attemptSeed);
                LevelGraph candidate = BuildTree(settings, random);
                if (candidate == null)
                {
                    lastError = "前沿耗尽，未能生成目标房间数。";
                    continue;
                }
                if (!AssignRoomTypes(candidate, settings, random, out lastError)) continue;
                if (!LevelGraphValidator.Validate(candidate, settings, out lastError)) continue;

                graph = candidate;
                error = null;
                return true;
            }

            graph = null;
            error = $"尝试 {settings.MaxGenerationAttempts} 次后仍未生成有效地图：{lastError}";
            return false;
        }

        private static LevelGraph BuildTree(LevelGenerationSettings settings, System.Random random)
        {
            int targetCount = random.Next(settings.MinRoomCount, settings.MaxRoomCount + 1);
            LevelGraph graph = new LevelGraph();
            RoomNode start = graph.AddNode(Vector2Int.zero, 0);
            graph.StartId = start.Id;

            while (graph.Count < targetCount)
            {
                List<FrontierCandidate> candidates = CollectFrontier(graph);
                if (candidates.Count == 0) return null;

                FrontierCandidate selected = candidates[random.Next(candidates.Count)];
                RoomNode parent = graph.GetNode(selected.ParentId);
                RoomNode child = graph.AddNode(selected.Position, parent.Depth + 1);
                graph.Connect(parent, selected.Direction, child);
            }
            return graph;
        }

        private static List<FrontierCandidate> CollectFrontier(LevelGraph graph)
        {
            List<FrontierCandidate> result = new List<FrontierCandidate>();
            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                foreach (RoomDirection direction in RoomDirectionUtility.All)
                {
                    Vector2Int position = node.GridPosition + RoomDirectionUtility.ToOffset(direction);
                    if (graph.TryGetNodeAt(position, out _)) continue;
                    result.Add(new FrontierCandidate(node.Id, direction, position));
                }
            }
            return result;
        }

        private static bool AssignRoomTypes(
            LevelGraph graph,
            LevelGenerationSettings settings,
            System.Random random,
            out string error)
        {
            for (int i = 0; i < graph.Count; i++) graph.Nodes[i].Type = RoomType.Normal;
            graph.GetNode(graph.StartId).Type = RoomType.Start;

            List<RoomNode> farthestLeaves = new List<RoomNode>();
            int maxDepth = -1;
            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                if (!node.IsLeaf || node.Id == graph.StartId) continue;
                if (node.Depth > maxDepth)
                {
                    maxDepth = node.Depth;
                    farthestLeaves.Clear();
                    farthestLeaves.Add(node);
                }
                else if (node.Depth == maxDepth)
                {
                    farthestLeaves.Add(node);
                }
            }
            if (maxDepth < settings.MinBossDepth || farthestLeaves.Count == 0)
            {
                error = "没有足够远的叶节点可用作 Boss 房。";
                return false;
            }

            RoomNode boss = farthestLeaves[random.Next(farthestLeaves.Count)];
            boss.Type = RoomType.Boss;
            graph.BossId = boss.Id;

            List<RoomNode> specialLeaves = new List<RoomNode>();
            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                if (node.IsLeaf && node.Id != graph.StartId && node.Id != graph.BossId)
                    specialLeaves.Add(node);
            }
            Shuffle(specialLeaves, random);

            int requiredLeaves = settings.TreasureRoomCount + settings.RecoveryRoomCount;
            if (specialLeaves.Count < requiredLeaves)
            {
                error = $"特殊叶节点不足，需要 {requiredLeaves} 个，实际只有 {specialLeaves.Count} 个。";
                return false;
            }

            int cursor = 0;
            for (int i = 0; i < settings.TreasureRoomCount; i++)
                specialLeaves[cursor++].Type = RoomType.Treasure;
            for (int i = 0; i < settings.RecoveryRoomCount; i++)
                specialLeaves[cursor++].Type = RoomType.Recovery;

            List<RoomNode> eliteCandidates = new List<RoomNode>();
            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                if (node.Type == RoomType.Normal && node.Depth >= settings.MinEliteDepth)
                    eliteCandidates.Add(node);
            }
            Shuffle(eliteCandidates, random);
            if (eliteCandidates.Count < settings.EliteRoomCount)
            {
                error = "满足深度要求的精英房候选不足。";
                return false;
            }
            for (int i = 0; i < settings.EliteRoomCount; i++)
                eliteCandidates[i].Type = RoomType.Elite;

            error = null;
            return true;
        }

        private static void Shuffle<T>(IList<T> list, System.Random random)
        {
            for (int i = list.Count - 1; i > 0; i--)
            {
                int j = random.Next(i + 1);
                T temp = list[i];
                list[i] = list[j];
                list[j] = temp;
            }
        }
    }
}
```

生成器分两段：

1. `BuildTree` 从 `(0,0)` 开始，每次从全部可用前沿随机扩展一个新节点；
2. `AssignRoomTypes` 把最远叶节点设为 Boss，再把宝箱和恢复放进其他叶节点，最后从满足深度的普通节点里抽精英房。

一次随机生长不一定有足够叶节点，也不一定有真正分岔。这个情况不是异常，而是“候选图不满足玩法约束”；生成器会使用从原 Seed 稳定派生的尝试 Seed 重试。

### 2.6 新建 `Assets/Scripts/Level/Generation/LevelGraphValidator.cs`

```csharp
using System.Collections.Generic;
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>集中验证生成图的连通性、唯一性和玩法约束。</summary>
    public static class LevelGraphValidator
    {
        public static bool Validate(LevelGraph graph, LevelGenerationSettings settings, out string error)
        {
            if (graph == null)
            {
                error = "关卡图为空。";
                return false;
            }
            if (graph.Count < settings.MinRoomCount || graph.Count > settings.MaxRoomCount)
            {
                error = $"房间数 {graph.Count} 不在配置范围内。";
                return false;
            }
            if (graph.StartId < 0 || graph.BossId < 0)
            {
                error = "起始房或 Boss 房未指定。";
                return false;
            }

            HashSet<Vector2Int> positions = new HashSet<Vector2Int>();
            int directedEdges = 0;
            int branchRooms = 0;
            int treasures = 0;
            int recoveries = 0;
            int elites = 0;

            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                if (!positions.Add(node.GridPosition))
                {
                    error = $"坐标 {node.GridPosition} 被重复占用。";
                    return false;
                }
                if (node.Degree >= 3) branchRooms++;
                if (node.Type == RoomType.Treasure) treasures++;
                else if (node.Type == RoomType.Recovery) recoveries++;
                else if (node.Type == RoomType.Elite) elites++;

                foreach (RoomDirection direction in RoomDirectionUtility.All)
                {
                    int neighborId = node.GetNeighborId(direction);
                    if (neighborId < 0) continue;
                    directedEdges++;
                    RoomNode neighbor = graph.GetNode(neighborId);
                    if (neighbor == null)
                    {
                        error = $"Room {node.Id} 指向不存在的 Room {neighborId}。";
                        return false;
                    }
                    if (neighbor.GridPosition != node.GridPosition + RoomDirectionUtility.ToOffset(direction))
                    {
                        error = $"Room {node.Id} 的 {direction} 邻居不在相邻网格。";
                        return false;
                    }
                    if (neighbor.GetNeighborId(RoomDirectionUtility.Opposite(direction)) != node.Id)
                    {
                        error = $"Room {node.Id} 与 Room {neighborId} 的连接不是双向的。";
                        return false;
                    }
                }
            }

            if (directedEdges / 2 != graph.Count - 1)
            {
                error = "首版地图必须是一棵树，连接数应为房间数减一。";
                return false;
            }
            if (branchRooms < settings.MinBranchRoomCount)
            {
                error = $"有效分岔房只有 {branchRooms} 个。";
                return false;
            }

            RoomNode start = graph.GetNode(graph.StartId);
            RoomNode boss = graph.GetNode(graph.BossId);
            if (start == null || start.Type != RoomType.Start)
            {
                error = "起始节点类型错误。";
                return false;
            }
            if (boss == null || boss.Type != RoomType.Boss || boss.Depth < settings.MinBossDepth)
            {
                error = "Boss 房类型或深度不满足配置。";
                return false;
            }
            if (treasures != settings.TreasureRoomCount
                || recoveries != settings.RecoveryRoomCount
                || elites != settings.EliteRoomCount)
            {
                error = "特殊房数量与配置不一致。";
                return false;
            }

            HashSet<int> visited = new HashSet<int>();
            Queue<int> queue = new Queue<int>();
            queue.Enqueue(graph.StartId);
            visited.Add(graph.StartId);
            while (queue.Count > 0)
            {
                RoomNode node = graph.GetNode(queue.Dequeue());
                foreach (RoomDirection direction in RoomDirectionUtility.All)
                {
                    int neighborId = node.GetNeighborId(direction);
                    if (neighborId >= 0 && visited.Add(neighborId)) queue.Enqueue(neighborId);
                }
            }
            if (visited.Count != graph.Count)
            {
                error = $"地图不连通，只能到达 {visited.Count}/{graph.Count} 个房间。";
                return false;
            }

            error = null;
            return true;
        }
    }
}
```

验证器集中检查：

- 房间总数在范围内；
- 坐标唯一；
- 邻接方向正确且双向；
- 边数等于 `N - 1`；
- 全图从起点可达；
- Boss 深度、分岔数量和特殊房数量符合配置。

生成算法“理论上应该正确”不等于可以省掉验证器。以后改规则时，验证器就是防止沉默地产出坏图的安全网。

### 2.7 新建 `Assets/Scripts/Debug/Debugging/LevelGraphDebugView.cs`

注意命名空间是 `Game.Debugging`，不是 `Game.Debug`。它是关卡的调试视图，不属于统一日志系统本身。

```csharp
using Game.Core;
using Game.Debug;
using Game.Level;
using UnityEngine;

namespace Game.Debugging
{
    /// <summary>开发期在 Scene 视图预览纯数据地图，并批量验证 Seed。</summary>
    public sealed class LevelGraphDebugView : MonoBehaviour
    {
        [SerializeField] private LevelGenerationSettings settings;
        [SerializeField] private int previewSeed = 12345;
        [SerializeField, Min(1)] private int validationSeedCount = 100;
        [SerializeField] private float gizmoSpacing = 3f;
        [SerializeField] private bool generateOnStart = true;

        private LevelGraph graph;

        private void Start()
        {
            if (generateOnStart) GeneratePreview();
        }

        [ContextMenu("Generate Preview")]
        public void GeneratePreview()
        {
            if (LevelGraphGenerator.TryGenerate(settings, previewSeed, out graph, out string error))
                GameDebug.Log(DebugCategory.Level, $"地图预览成功：{graph.BuildSignature()}", this);
            else
                GameDebug.Error(DebugCategory.Level, error, this);
        }

        [ContextMenu("Validate Seed Range")]
        public void ValidateSeedRange()
        {
            for (int i = 0; i < validationSeedCount; i++)
            {
                if (!LevelGraphGenerator.TryGenerate(settings, previewSeed + i, out LevelGraph first, out string error))
                {
                    GameDebug.Error(DebugCategory.Level, $"Seed={previewSeed + i}：{error}", this);
                    return;
                }
                if (!LevelGraphGenerator.TryGenerate(settings, previewSeed + i, out LevelGraph second, out error)
                    || first.BuildSignature() != second.BuildSignature())
                {
                    GameDebug.Error(DebugCategory.Level, $"Seed={previewSeed + i} 不能稳定复现。", this);
                    return;
                }
            }
            GameDebug.Log(DebugCategory.Level, $"连续 {validationSeedCount} 个 Seed 验证通过。", this);
        }

        private void OnDrawGizmos()
        {
            if (graph == null) return;
            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                Vector3 position = new Vector3(
                    node.GridPosition.x * gizmoSpacing,
                    node.GridPosition.y * gizmoSpacing,
                    0f) + transform.position;
                Gizmos.color = GetColor(node.Type);
                Gizmos.DrawCube(position, Vector3.one * 0.7f);

                DrawConnection(node, RoomDirection.East, position);
                DrawConnection(node, RoomDirection.North, position);
            }
        }

        private void DrawConnection(RoomNode node, RoomDirection direction, Vector3 from)
        {
            int neighborId = node.GetNeighborId(direction);
            if (neighborId < 0) return;
            RoomNode neighbor = graph.GetNode(neighborId);
            Vector3 to = new Vector3(
                neighbor.GridPosition.x * gizmoSpacing,
                neighbor.GridPosition.y * gizmoSpacing,
                0f) + transform.position;
            Gizmos.color = Color.white;
            Gizmos.DrawLine(from, to);
        }

        private static Color GetColor(RoomType type)
        {
            switch (type)
            {
                case RoomType.Start: return Color.cyan;
                case RoomType.Elite: return Color.magenta;
                case RoomType.Treasure: return Color.yellow;
                case RoomType.Recovery: return Color.green;
                case RoomType.Boss: return Color.red;
                default: return Color.gray;
            }
        }
    }
}
```

### 2.8 创建配置并验证

1. `Create > Game > Level Generation Settings`，创建 `Assets/Data/Rooms/LevelGenerationSettings.asset`。
2. 场景中新建空物体 `LevelGraphDebugView`，挂同名脚本并连接 Settings。
3. 进入 Play，Scene 视图应看到彩色方块和连接线。
4. 组件右上角菜单执行 `Validate Seed Range`，先验证连续 100 个 Seed。
5. 把 Preview Seed 改成几个明显不同的值；结构应变化，但相同值每次签名一致。

颜色约定：起点青、普通灰、精英紫、宝箱黄、恢复绿、Boss 红。

> ✅ **步骤 2 验收标准**：项目仍运行旧固定关卡；纯数据地图能预览；连续 100 个 Seed 全部通过；同 Seed 两次签名完全一致；没有任何房间 GameObject 被生成。

---

## 步骤 3：把数据图建造成实体房间与通道

**这一步做什么**：创建统一的灰盒房间模板，让每个图节点实例化一间房；每条边实例化一段可行走通道。完成后旧的场景三房和传送流程将被正式替换。

这是本阶段第一次大规模重构。请先复制当前场景作为备份，例如 `Main_V1_5_2_Backup.unity`，再在正式场景中操作。

### 3.1 新建 `Assets/Scripts/Level/Building/RoomCatalog.cs`

```csharp
using System;
using System.Collections.Generic;
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>按房间类型保存 RoomConfig 候选池，并执行深度与权重选择。</summary>
    [CreateAssetMenu(fileName = "RoomCatalog", menuName = "Game/Room Catalog")]
    public sealed class RoomCatalog : ScriptableObject
    {
        [SerializeField] private RoomConfig[] startRooms;
        [SerializeField] private RoomConfig[] normalRooms;
        [SerializeField] private RoomConfig[] eliteRooms;
        [SerializeField] private RoomConfig[] treasureRooms;
        [SerializeField] private RoomConfig[] recoveryRooms;
        [SerializeField] private RoomConfig[] bossRooms;

        public bool TryChoose(RoomType type, int depth, System.Random random, out RoomConfig config, out bool usedDepthFallback)
        {
            RoomConfig[] pool = GetPool(type);
            List<RoomConfig> all = CollectNonNull(pool);
            if (all.Count == 0)
            {
                config = null;
                usedDepthFallback = false;
                return false;
            }

            List<RoomConfig> depthMatches = new List<RoomConfig>();
            for (int i = 0; i < all.Count; i++)
                if (all[i].SupportsDepth(depth)) depthMatches.Add(all[i]);

            usedDepthFallback = depthMatches.Count == 0;
            config = ChooseWeighted(usedDepthFallback ? all : depthMatches, random);
            return config != null;
        }

        private RoomConfig[] GetPool(RoomType type)
        {
            switch (type)
            {
                case RoomType.Start: return startRooms;
                case RoomType.Normal: return normalRooms;
                case RoomType.Elite: return eliteRooms;
                case RoomType.Treasure: return treasureRooms;
                case RoomType.Recovery: return recoveryRooms;
                case RoomType.Boss: return bossRooms;
                default: return Array.Empty<RoomConfig>();
            }
        }

        private static List<RoomConfig> CollectNonNull(RoomConfig[] pool)
        {
            List<RoomConfig> result = new List<RoomConfig>();
            if (pool == null) return result;
            for (int i = 0; i < pool.Length; i++)
                if (pool[i] != null) result.Add(pool[i]);
            return result;
        }

        private static RoomConfig ChooseWeighted(IReadOnlyList<RoomConfig> candidates, System.Random random)
        {
            int totalWeight = 0;
            for (int i = 0; i < candidates.Count; i++)
                totalWeight += Mathf.Max(1, candidates[i].selectionWeight);

            int roll = random.Next(totalWeight);
            for (int i = 0; i < candidates.Count; i++)
            {
                roll -= Mathf.Max(1, candidates[i].selectionWeight);
                if (roll < 0) return candidates[i];
            }
            return candidates[candidates.Count - 1];
        }
    }
}
```

`RoomCatalog` 只负责“这个类型和深度有哪些内容配置可选”。它不保存房间预制体，因为首版所有房间共用同一个几何模板；以后有不同尺寸或形状时，再把几何模板升级为独立目录。

### 3.2 新建 `Assets/Scripts/Level/Rooms/RoomConnection.cs`

```csharp
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>房间一个方向的墙面开口、战斗门和通道锚点。</summary>
    public sealed class RoomConnection : MonoBehaviour
    {
        [SerializeField] private RoomDirection direction;
        [Tooltip("没有邻居时覆盖门洞的墙体")]
        [SerializeField] private GameObject closedWall;
        [SerializeField] private Door gate;
        [SerializeField] private Transform corridorAnchor;

        public RoomDirection Direction => direction;
        public Door Gate => gate;
        public Transform CorridorAnchor => corridorAnchor != null ? corridorAnchor : transform;
        public bool IsConnected { get; private set; }

        public void Configure(bool connected)
        {
            IsConnected = connected;
            if (closedWall != null) closedWall.SetActive(!connected);
            if (gate != null)
            {
                gate.gameObject.SetActive(connected);
                if (connected) gate.Unlock();
            }
        }
    }
}
```

每个房间有 North/East/South/West 四个连接组件：

- 有邻居：隐藏门洞填充墙，启用 Gate；
- 无邻居：显示填充墙，隐藏 Gate；
- `CorridorAnchor` 是通道两端的几何对齐点。

### 3.3 新建 `Assets/Scripts/Level/Building/DungeonBuilder.cs`

```csharp
using System.Collections.Generic;
using Game.Core;
using Game.Debug;
using UnityEngine;

namespace Game.Level
{
    public sealed class DungeonBuildResult
    {
        public readonly Dictionary<int, Room> Rooms;
        public readonly Room StartRoom;

        public DungeonBuildResult(Dictionary<int, Room> rooms, Room startRoom)
        {
            Rooms = rooms;
            StartRoom = startRoom;
        }
    }

    /// <summary>把 LevelGraph 建造成世界空间中的房间、门洞和实体通道。</summary>
    public sealed class DungeonBuilder : MonoBehaviour
    {
        [SerializeField] private Room roomPrefab;
        [SerializeField] private RoomCatalog roomCatalog;
        [SerializeField] private Transform dungeonRoot;

        [Header("网格间距")]
        [SerializeField] private float horizontalSpacing = 28f;
        [SerializeField] private float verticalSpacing = 20f;

        [Header("通道预制体")]
        [SerializeField] private GameObject horizontalCorridorPrefab;
        [SerializeField] private GameObject verticalCorridorPrefab;

        public DungeonBuildResult Build(LevelGraph graph, int seed)
        {
            if (graph == null || roomPrefab == null || roomCatalog == null)
            {
                GameDebug.Error(DebugCategory.Level, "DungeonBuilder 缺少关卡图、Room Prefab 或 RoomCatalog。", this);
                return null;
            }

            Transform parent = dungeonRoot != null ? dungeonRoot : transform;
            System.Random contentRandom = new System.Random(unchecked(seed ^ 0x2C9277B5));
            Dictionary<int, Room> rooms = new Dictionary<int, Room>();

            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                Vector3 worldPosition = new Vector3(
                    node.GridPosition.x * horizontalSpacing,
                    node.GridPosition.y * verticalSpacing,
                    0f);
                Room room = Instantiate(roomPrefab, worldPosition, Quaternion.identity, parent);
                room.name = $"Room_{node.Id}_{node.Type}_{node.GridPosition.x}_{node.GridPosition.y}";

                RoomConfig config = null;
                if (!roomCatalog.TryChoose(node.Type, node.Depth, contentRandom, out config, out bool fallback))
                {
                    GameDebug.Error(DebugCategory.Level, $"{node.Type} 没有可用的 RoomConfig。", room);
                }
                else if (fallback)
                {
                    GameDebug.Warning(
                        DebugCategory.Level,
                        $"{node.Type} 在深度 {node.Depth} 没有匹配配置，已回退到该类型的任意配置。",
                        room);
                }

                room.Initialize(node, config);
                rooms.Add(node.Id, room);
            }

            ConfigureConnectionsAndCorridors(graph, rooms, parent);
            rooms.TryGetValue(graph.StartId, out Room startRoom);
            return new DungeonBuildResult(rooms, startRoom);
        }

        private void ConfigureConnectionsAndCorridors(
            LevelGraph graph,
            IReadOnlyDictionary<int, Room> rooms,
            Transform parent)
        {
            for (int i = 0; i < graph.Count; i++)
            {
                RoomNode node = graph.Nodes[i];
                Room room = rooms[node.Id];
                foreach (RoomDirection direction in RoomDirectionUtility.All)
                    room.ConfigureConnection(direction, node.GetNeighborId(direction) >= 0);

                CreateCorridorIfNeeded(node, RoomDirection.East, horizontalCorridorPrefab, rooms, parent);
                CreateCorridorIfNeeded(node, RoomDirection.North, verticalCorridorPrefab, rooms, parent);
            }
        }

        private void CreateCorridorIfNeeded(
            RoomNode node,
            RoomDirection direction,
            GameObject corridorPrefab,
            IReadOnlyDictionary<int, Room> rooms,
            Transform parent)
        {
            int neighborId = node.GetNeighborId(direction);
            if (neighborId < 0) return;
            if (corridorPrefab == null)
            {
                GameDebug.Error(DebugCategory.Level, $"{direction} 通道预制体未连接。", this);
                return;
            }

            Room fromRoom = rooms[node.Id];
            Room toRoom = rooms[neighborId];
            RoomConnection from = fromRoom.GetConnection(direction);
            RoomConnection to = toRoom.GetConnection(RoomDirectionUtility.Opposite(direction));
            if (from == null || to == null)
            {
                GameDebug.Error(DebugCategory.Level, $"Room {node.Id} 或 Room {neighborId} 缺少 {direction} 连接组件。", this);
                return;
            }

            Vector3 middle = (from.CorridorAnchor.position + to.CorridorAnchor.position) * 0.5f;
            GameObject corridor = Instantiate(corridorPrefab, middle, Quaternion.identity, parent);
            corridor.name = $"Corridor_{node.Id}_{neighborId}";
        }
    }
}
```

Builder 只对 East 和 North 创建通道，是为了避免同一条无向边被生成两次。West/South 仍会配置门洞，只是不重复实例化连接物。

### 3.4 大规模重写 `Assets/Scripts/Level/Rooms/Room.cs`

```csharp
using System;
using System.Collections.Generic;
using Game.Core;
using Game.Entities;
using UnityEngine;

namespace Game.Level
{
    /// <summary>一个实体房间：延迟生成内容、管理战斗门、奖励等待和固定相机。</summary>
    public sealed class Room : MonoBehaviour
    {
        [SerializeField] private RoomConfig config;
        [SerializeField] private Transform playerSpawnPoint;
        [SerializeField] private RoomConnection[] connections;
        [SerializeField] private GameObject roomCamera;
        [SerializeField] private Transform contentParent;

        public event Action<Room> Entered;
        public event Action<Room> Exited;
        public event Action<Room> RoomCleared;
        public event Action<Room> RewardRequested;

        private readonly List<Health> trackedEnemies = new List<Health>();
        private int aliveCount;
        private bool initialized;
        private bool spawned;
        private bool playerInside;
        private bool rewardPending;
        private bool recoveryConsumed;

        public RoomNode Node { get; private set; }
        public RoomConfig Config => config;
        public RoomType Type => Node != null ? Node.Type : config != null ? config.type : RoomType.Normal;
        public Transform PlayerSpawnPoint => playerSpawnPoint != null ? playerSpawnPoint : transform;
        public bool IsCleared { get; private set; }

        public void Initialize(RoomNode node, RoomConfig roomConfig)
        {
            Node = node;
            config = roomConfig;
            initialized = node != null;
            if (roomCamera != null) roomCamera.SetActive(false);
        }

        public void ConfigureConnection(RoomDirection direction, bool connected)
        {
            RoomConnection connection = GetConnection(direction);
            if (connection != null) connection.Configure(connected);
        }

        public RoomConnection GetConnection(RoomDirection direction)
        {
            if (connections == null) return null;
            for (int i = 0; i < connections.Length; i++)
                if (connections[i] != null && connections[i].Direction == direction) return connections[i];
            return null;
        }

        public void HandlePlayerEntered()
        {
            if (!initialized || playerInside) return;
            playerInside = true;
            Entered?.Invoke(this);

            if (spawned) return;
            spawned = true;
            SpawnContents();
            if (aliveCount > 0) LockConnectedGates();
            else CompleteEncounter();
        }

        public void HandlePlayerExited()
        {
            if (!playerInside) return;
            playerInside = false;
            Exited?.Invoke(this);
        }

        public void CompleteReward()
        {
            if (!rewardPending) return;
            rewardPending = false;
            UnlockConnectedGates();
        }

        public bool TryConsumeRecovery(out int amount)
        {
            amount = 0;
            if (recoveryConsumed || Type != RoomType.Recovery || config == null || config.healAmount <= 0)
                return false;
            recoveryConsumed = true;
            amount = config.healAmount;
            return true;
        }

        public void SetCameraActive(bool active)
        {
            if (roomCamera != null) roomCamera.SetActive(active);
        }

        private void SpawnContents()
        {
            if (config == null) return;
            Transform parent = contentParent != null ? contentParent : transform;

            if (config.enemySpawns != null)
            {
                foreach (EnemySpawn spawn in config.enemySpawns)
                {
                    Vector3 worldPosition = transform.position + (Vector3)spawn.localPosition;
                    GameObject enemy = EnemyFactory.Create(spawn.prefab, worldPosition, parent);
                    if (enemy == null || !enemy.TryGetComponent(out Health health)) continue;
                    health.Died += OnEnemyDied;
                    trackedEnemies.Add(health);
                    aliveCount++;
                }
            }

            if (config.pickupSpawns != null)
            {
                foreach (PickupSpawn spawn in config.pickupSpawns)
                {
                    Vector3 worldPosition = transform.position + (Vector3)spawn.localPosition;
                    EnemyFactory.Create(spawn.prefab, worldPosition, parent);
                }
            }
        }

        private void OnEnemyDied()
        {
            aliveCount--;
            if (aliveCount <= 0) CompleteEncounter();
        }

        private void CompleteEncounter()
        {
            if (IsCleared) return;
            IsCleared = true;
            RoomCleared?.Invoke(this);

            if (config != null && config.grantsUpgradeReward)
            {
                rewardPending = true;
                LockConnectedGates();
                RewardRequested?.Invoke(this);
            }
            else
            {
                UnlockConnectedGates();
            }
        }

        private void LockConnectedGates()
        {
            if (connections == null) return;
            for (int i = 0; i < connections.Length; i++)
                if (connections[i] != null && connections[i].IsConnected && connections[i].Gate != null)
                    connections[i].Gate.Lock();
        }

        private void UnlockConnectedGates()
        {
            if (connections == null) return;
            for (int i = 0; i < connections.Length; i++)
                if (connections[i] != null && connections[i].IsConnected && connections[i].Gate != null)
                    connections[i].Gate.Unlock();
        }

        private void OnDestroy()
        {
            for (int i = 0; i < trackedEnemies.Count; i++)
                if (trackedEnemies[i] != null) trackedEnemies[i].Died -= OnEnemyDied;
            trackedEnemies.Clear();
        }
    }
}
```

旧 Room 的“进入就刷怪”思路保留，但增加了：

- `RoomNode` 身份与四向连接；
- 进入/离开事件；
- 战斗期间封锁所有已连接 Gate；
- 清房后可能先等待强化选择，选完才开门；
- 恢复房效果只消费一次；
- 重访已生成房间不会再刷怪。

### 3.5 大规模重写 `Assets/Scripts/Level/LevelManager.cs`

```csharp
using System.Collections.Generic;
using Game.Core;
using Game.Debug;
using Game.Entities;
using Game.Rewards;
using UnityEngine;

namespace Game.Level
{
    /// <summary>生成并建造本局地图，协调房间事件、相机、恢复与最终通关。</summary>
    public sealed class LevelManager : MonoBehaviour
    {
        [SerializeField] private LevelGenerationSettings generationSettings;
        [SerializeField] private DungeonBuilder dungeonBuilder;
        [SerializeField] private GameObject explorationCamera;
        [SerializeField] private RunUpgradeManager upgradeManager;

        private readonly List<Room> runtimeRooms = new List<Room>();
        private LevelGraph graph;
        private Room currentRoom;

        private void Start()
        {
            int seed = RunManager.Instance != null ? RunManager.Instance.CurrentSeed : 12345;
            if (!LevelGraphGenerator.TryGenerate(generationSettings, seed, out graph, out string error))
            {
                GameDebug.Error(DebugCategory.Level, error, this);
                enabled = false;
                return;
            }

            DungeonBuildResult buildResult = dungeonBuilder != null ? dungeonBuilder.Build(graph, seed) : null;
            if (buildResult == null || buildResult.StartRoom == null)
            {
                GameDebug.Error(DebugCategory.Level, "实体地图建造失败。", this);
                enabled = false;
                return;
            }

            foreach (Room room in buildResult.Rooms.Values)
            {
                runtimeRooms.Add(room);
                room.Entered += OnRoomEntered;
                room.Exited += OnRoomExited;
                room.RoomCleared += OnRoomCleared;
                room.RewardRequested += OnRewardRequested;
            }

            if (explorationCamera != null) explorationCamera.SetActive(true);
            EventBus.Publish(new LevelStartedEvent(graph.CreateMapSnapshot()));
            PositionPlayer(buildResult.StartRoom.PlayerSpawnPoint);
            buildResult.StartRoom.HandlePlayerEntered();
            GameDebug.Log(
                DebugCategory.Level,
                $"地图生成完成：Seed={seed}，Rooms={graph.Count}，Signature={graph.BuildSignature()}。",
                this);
        }

        private void OnDisable()
        {
            for (int i = 0; i < runtimeRooms.Count; i++)
            {
                Room room = runtimeRooms[i];
                if (room == null) continue;
                room.Entered -= OnRoomEntered;
                room.Exited -= OnRoomExited;
                room.RoomCleared -= OnRoomCleared;
                room.RewardRequested -= OnRewardRequested;
            }
            runtimeRooms.Clear();
        }

        private void OnRoomEntered(Room room)
        {
            if (currentRoom != null && currentRoom != room) currentRoom.SetCameraActive(false);
            currentRoom = room;
            if (explorationCamera != null) explorationCamera.SetActive(false);
            room.SetCameraActive(true);
            EventBus.Publish(new RoomEnteredEvent(room.Node.Id));

            if (room.TryConsumeRecovery(out int healAmount))
            {
                GameObject player = GameManager.Instance != null ? GameManager.Instance.Player : null;
                if (player != null && player.TryGetComponent(out Health health)) health.Heal(healAmount);
            }
        }

        private void OnRoomExited(Room room)
        {
            if (currentRoom != room) return;
            room.SetCameraActive(false);
            currentRoom = null;
            if (explorationCamera != null) explorationCamera.SetActive(true);
        }

        private void OnRoomCleared(Room room)
        {
            EventBus.Publish(new RoomClearedEvent(room.Node.Id));
            if (room.Type != RoomType.Boss) return;

            if (RunManager.Instance != null) RunManager.Instance.CompleteRun();
            else EventBus.Publish(new LevelCompletedEvent());
        }

        private void OnRewardRequested(Room room)
        {
            if (upgradeManager != null) upgradeManager.PresentChoices(room.CompleteReward);
            else
            {
                GameDebug.Warning(DebugCategory.Level, "没有 RunUpgradeManager，奖励房直接解锁。", this);
                room.CompleteReward();
            }
        }

        private static void PositionPlayer(Transform spawnPoint)
        {
            GameObject player = GameManager.Instance != null
                ? GameManager.Instance.Player
                : GameObject.FindGameObjectWithTag("Player");
            if (player == null || spawnPoint == null) return;

            if (player.TryGetComponent(out Rigidbody2D rb))
            {
                rb.position = spawnPoint.position;
                rb.linearVelocity = Vector2.zero;
            }
            else
            {
                player.transform.position = spawnPoint.position;
            }
        }
    }
}
```

新 LevelManager 不再拥有 `Room[]` 顺序数组，也不再订阅门事件。它只负责：

- 读取 Run Seed；
- 生成 `LevelGraph` 并交给 Builder；
- 订阅运行时 Room 的局部事件；
- 把局部房间事件桥接成全局 EventBus 事件；
- 管理相机、恢复、奖励和 Boss 通关。

### 3.6 创建灰盒 Room Prefab

推荐先做固定尺寸，不要在这一步同时引入 Tilemap 规则瓦片。以下数值与 Reference 默认间距配套：

```text
房间中心间距：水平 28，垂直 20
房间内部尺寸：约 18 × 10
左右门洞中心：x = ±9
上下门洞中心：y = ±5
水平通道：从 x=-5 到 x=5，约 10 单位长
垂直通道：从 y=-5 到 y=5，约 10 单位长
```

`GeneratedRoom.prefab` 建议结构：

```text
GeneratedRoom (Room)
├── Floor
├── Walls
│   ├── PermanentWalls          # 四边门洞之外的墙，始终存在
│   ├── NorthConnection (RoomConnection: North)
│   │   ├── ClosedWall          # 无邻居时填门洞
│   │   ├── Gate (Door + BoxCollider2D)
│   │   └── CorridorAnchor
│   ├── EastConnection
│   ├── SouthConnection
│   └── WestConnection
├── EncounterTrigger (BoxCollider2D Is Trigger + RoomEncounterTrigger)
├── PlayerSpawnPoint
├── ContentRoot
└── RoomCamera (CinemachineCamera + ScreenShake，初始禁用)
```

配置要点：

1. `EncounterTrigger` 只覆盖房间内部，不覆盖通道；否则玩家刚踏进通道就会被误判仍在房间。
2. Gate 的 `BoxCollider2D` **不要勾 Is Trigger**。门锁时启用 Collider，解锁时禁用。
3. Gate 不需要负责换房；玩家直接穿过解锁门洞走进通道。
4. 四个 `RoomConnection` 的 `Direction` 不能重复或填错。
5. `connections` 数组连接四个组件；`contentParent` 指向 `ContentRoot`。
6. 房间外墙和通道边墙必须有普通 Collider2D，确保玩家不能从地图外绕过去。
7. `RoomCamera` 初始禁用，`Room.Initialize` 也会再次保证它关闭。

### 3.7 创建两个 Corridor Prefab

建立 `HorizontalCorridor.prefab` 与 `VerticalCorridor.prefab`。每个至少包含：

- 地板 Sprite；
- 两侧不可穿越的 BoxCollider2D 墙；
- 足以连接两个 `CorridorAnchor` 的固定长度。

Reference Builder 只放到两个 Anchor 的中点，**不会自动缩放通道**。因此 Prefab 长度必须与房间尺寸和 `horizontalSpacing / verticalSpacing` 精确配套。先用 Scene Gizmos 对齐，再保存 Prefab。

### 3.8 配置 Builder 并替换场景旧房间

1. 场景中保留一个 `LevelManager`，增加 `DungeonBuilder` 组件或建立独立 `DungeonBuilder` 物体。
2. 新建空物体 `DungeonRoot`，连接到 Builder，方便运行时统一查看生成内容。
3. 连接 `GeneratedRoom`、`RoomCatalog` 和两个 Corridor Prefab。
4. 暂时给 Catalog 的六类数组都放入可用配置；特殊配置会在步骤 6 正式整理。
5. 删除或禁用场景中旧的三个 Room 实例，避免固定房间与生成房间重叠。
6. 替换 `LevelManager` 后连接 `generationSettings` 与 `dungeonBuilder`。
7. 同步 GameEvents 的关卡最终版本；删除对 `DoorEnteredEvent` 的所有依赖。

> ✅ **步骤 3 验收标准**：进入 Play 后一次生成 9～12 个真实房间；所有连接都有可行走通道；没有邻居的门洞被墙封住；玩家出生在 Start 房；同 Seed 的世界布局与内容配置选择一致；游戏不再通过 Door 传送。

---

## 步骤 4：房间触发、战斗门、重访与奖励等待

**这一步做什么**：把“地图能走”变成“房间流程能玩”。进入未访问战斗房才刷怪并封门；清空后开门；清过的房间可以安全重访；有奖励的房间在玩家完成选择之前继续锁门。

### 4.1 重写 `Assets/Scripts/Level/Rooms/Door.cs`

```csharp
using UnityEngine;

namespace Game.Level
{
    /// <summary>房间战斗期间启用实体 Collider 封锁连接口，清空后重新开放。</summary>
    [RequireComponent(typeof(Collider2D))]
    public class Door : MonoBehaviour
    {
        [SerializeField] private SpriteRenderer spriteRenderer;
        [SerializeField] private Color lockedColor = new Color(0.8f, 0.2f, 0.2f);
        [SerializeField] private Color unlockedColor = new Color(0.2f, 0.8f, 0.3f);

        private Collider2D gateCollider;
        public bool IsLocked { get; private set; }

        private void Awake()
        {
            gateCollider = GetComponent<Collider2D>();
            Unlock();
        }

        public void Lock()
        {
            IsLocked = true;
            if (gateCollider != null) gateCollider.enabled = true;
            if (spriteRenderer != null) spriteRenderer.color = lockedColor;
        }
        public void Unlock()
        {
            IsLocked = false;
            if (gateCollider != null) gateCollider.enabled = false;
            if (spriteRenderer != null) spriteRenderer.color = unlockedColor;
        }
    }
}
```

旧 Door 的核心语义是“解锁后碰到 Player 就发换房事件”；新 Door 的语义只是“一块可开关的实体阻挡”。这不是改名，而是职责发生了根本变化。

需要特别检查：

- `Awake()` 调 `Unlock()`，保证地图刚建好时通路可走；
- `Lock()` 启用 Collider；`Unlock()` 禁用 Collider；
- `SpriteRenderer` 只是调试表现。未来换门动画时也不要让 Door 再知道目的房间。

### 4.2 新建 `Assets/Scripts/Level/Rooms/RoomEncounterTrigger.cs`

```csharp
using UnityEngine;

namespace Game.Level
{
    /// <summary>把玩家进入/离开房间内部区域转发给 Room。</summary>
    [RequireComponent(typeof(Collider2D))]
    public sealed class RoomEncounterTrigger : MonoBehaviour
    {
        [SerializeField] private Room room;

        private void OnTriggerEnter2D(Collider2D other)
        {
            if (other.CompareTag("Player")) room.HandlePlayerEntered();
        }

        private void OnTriggerExit2D(Collider2D other)
        {
            if (other.CompareTag("Player")) room.HandlePlayerExited();
        }
    }
}
```

触发器是物理层到领域对象 `Room` 的薄适配器。它不生成敌人、不操作相机、不发全局事件，只转发玩家进入/离开。

Inspector 中把 `room` 指向 Prefab 根上的 `Room`。如果忘记连接，这个脚本会在触发时空引用；这是 Prefab 制作阶段必须检查的固定引用。

### 4.3 完整房间生命周期

理解下面这张状态表，比只看 `bool` 更重要：

| 场景 | `spawned` | `IsCleared` | Gate | 结果 |
|---|---:|---:|---|---|
| 首次进入有敌房 | false → true | false | 锁 | 生成敌人并等待全部死亡 |
| 最后一名敌人死亡 | true | false → true | 视奖励而定 | 无奖励直接开门；有奖励继续锁门 |
| 选择强化后 | true | true | 开 | 调用 `CompleteReward()` |
| 首次进入空房 | false → true | false → true | 视奖励而定 | 不会出现“空房永远不开门” |
| 重访已清房 | true | true | 开 | 不刷怪、不重复发清房事件 |
| 首次进入恢复房 | false → true | true | 开 | `LevelManager` 只恢复一次生命 |

这里有两个容易漏掉的沉默问题：

1. **奖励必须属于清房后的第二阶段**。如果 `RoomCleared` 立刻开门，玩家可以在奖励 UI 打开时走出房间；Reference 用 `rewardPending` 把开门延后到选择完成。
2. **敌人事件必须退订**。房间销毁时仍存活的敌人可能持有 `OnEnemyDied` 委托；`OnDestroy` 统一解除，避免跨场景残留。

### 4.4 建议的 Play 验收顺序

1. 进入普通战斗房：四个实际连接方向全部封锁，没连接的方向仍是墙。
2. 尝试撞门：玩家不能穿过。
3. 杀到只剩一名敌人：门仍锁。
4. 杀死最后一名：没有奖励时立刻开；有奖励时保持锁定。
5. 完成奖励：全部连接门同时打开。
6. 离开后返回：不再刷怪，也不再锁门。
7. 清空一个敌人数组为空的特殊房：不会卡死。

> ✅ **步骤 4 验收标准**：首次进入、战斗封门、最后敌人死亡、奖励等待、开门和重访全部正确；不依赖房间顺序；从任意连接方向进入都得到相同行为。

---

## 步骤 5：房间固定镜头 + 通道跟随镜头

**这一步做什么**：保留当前固定房间镜头的阅读性，同时让玩家穿越通道时有连续跟随，而不是画面停留在刚离开的房间。

这里采用混合方案：

```text
玩家在房间内部 Trigger 中：RoomCamera active，ExplorationCamera inactive
玩家离开房间进入通道：RoomCamera inactive，ExplorationCamera active
玩家进入下一房间：ExplorationCamera inactive，新 RoomCamera active
```

### 5.1 创建 ExplorationCamera

1. 在场景中新增一个 Cinemachine Camera，命名 `ExplorationCamera`。
2. Tracking Target 指向 Player，Position Control 使用适合当前项目的 2D 跟随方式。
3. Lens 与现有 RoomCamera 保持一致，避免切换时画面突然缩放。
4. 添加现有 `ScreenShake` 组件。震屏订阅只对 active 相机生效。
5. 把它连接到 `LevelManager.explorationCamera`。

不要把 Main Camera 直接拖给该字段；这里控制的是 Cinemachine 虚拟相机 GameObject。

### 5.2 房间 Trigger 的边界

镜头切换是否自然，主要取决于 Trigger：

- Trigger 太大并伸进通道：跟随镜头迟迟不启用，玩家会走到固定镜头边缘；
- Trigger 太小：玩家在房间墙边移动时会反复进出，镜头频闪；
- 相邻房间 Trigger 重叠：进入新房间之前旧房间可能仍认为玩家在内部。

建议让 Trigger 比内墙范围缩进约 `0.2～0.4` 单位，并确保门洞外立即进入通道区域。

### 5.3 暂不增加相机专用事件

相机切换目前是 `LevelManager` 对房间局部事件的直接协调，因为它和运行时 Room 生命周期强相关。等未来出现观战、传送、过场或多玩家相机，再把“当前空间区域改变”抽成更通用的事件。现在额外造一层事件只会让开关顺序更难追踪。

> ✅ **步骤 5 验收标准**：房间内保持固定镜头；走入通道后相机平滑跟随；进入任意下一房间切到该房固定镜头；往返不会同时启用多台相机；房间和通道中击杀震屏都正常。

---

## 步骤 6：房间类型、深度约束与内容目录

**这一步做什么**：让拓扑上的 `RoomType` 真正影响房间内容，而不是只改变小地图颜色。

### 6.1 更新 `Assets/Scripts/Level/Rooms/RoomConfig.cs`

这是小规模字段扩展。为了方便核对 ScriptableObject 的最终数据结构，下面仍给出完整文件。

```csharp
using Game.Core;
using UnityEngine;

namespace Game.Level
{
    /// <summary>描述房间类型，以及敌人和拾取物关于房间中心的相对位置。</summary>
    [CreateAssetMenu(fileName = "NewRoom", menuName = "Game/Room Config")]
    public class RoomConfig : ScriptableObject
    {
        public string roomName = "Room";
        public RoomType type = RoomType.Normal;

        [Header("选择规则")]
        [Tooltip("节点深度低于该值时不会选择此配置")]
        [Min(0)] public int minDepth;
        [Tooltip("节点深度高于该值时不会选择此配置")]
        [Min(0)] public int maxDepth = 99;
        [Tooltip("同类型、同深度候选中的相对权重")]
        [Min(1)] public int selectionWeight = 1;

        [Header("完成奖励")]
        public bool grantsUpgradeReward;
        [Tooltip("恢复房首次进入时恢复的生命值；其他房间保持 0")]
        [Min(0)] public int healAmount;

        [Tooltip("敌人出生点(相对房间中心)")]
        public EnemySpawn[] enemySpawns;
        [Tooltip("道具生成点(相对房间中心)")]
        public PickupSpawn[] pickupSpawns;

        public bool SupportsDepth(int depth) => depth >= minDepth && depth <= maxDepth;
    }

    /// <summary>一项敌人预制体及其关于房间中心的相对位置。</summary>
    [System.Serializable]
    public struct EnemySpawn
    {
        public GameObject prefab;
        [Tooltip("关于房间中心的相对位置")]
        public Vector2 localPosition;
    }

    /// <summary>一项拾取物预制体及其关于房间中心的相对位置。</summary>
    [System.Serializable]
    public struct PickupSpawn
    {
        public GameObject prefab;
        [Tooltip("关于房间中心的相对位置")]
        public Vector2 localPosition;
    }
}
```

`RoomConfig.type` 与图节点类型必须一致。Catalog 已按类型分池，保留这个字段是为了资产自检和 Inspector 可读性，不应该在运行时反过来覆盖 `RoomNode.Type`。

### 6.2 建立最小配置矩阵

先复用现有敌人 Prefab，建立下列 RoomConfig。数量是最低可玩版本，不是最终内容量：

| 资产建议名 | Type | Depth | Reward | Heal | 内容建议 |
|---|---|---:|---:|---:|---|
| `Room_Start_01` | Start | 0～0 | 否 | 0 | 空房 |
| `Room_Normal_Early_01` | Normal | 1～3 | 是 | 0 | 2 近战 |
| `Room_Normal_Mid_01` | Normal | 2～99 | 是 | 0 | 近战 + 远程 |
| `Room_Normal_Bomber_01` | Normal | 3～99 | 是 | 0 | 远程 + 自爆 |
| `Room_Elite_01` | Elite | 2～99 | 是 | 0 | 更密集混合编队 |
| `Room_Treasure_01` | Treasure | 0～99 | 是 | 0 | 空房，进入即给奖励 |
| `Room_Recovery_01` | Recovery | 0～99 | 否 | 30 | 空房，首次进入回血 |
| `Room_Boss_01` | Boss | 4～99 | 否 | 0 | Boss |

权重示例：普通早期配置 `3`、混合配置 `2`、自爆配置 `1`。权重不是百分比；候选权重为 `3/2/1` 时，抽取概率分别是 `3/6`、`2/6`、`1/6`。

### 6.3 配置 RoomCatalog

创建 `Assets/Data/RoomCatalog.asset`，将每份配置放入对应类型数组。注意：

- 数组中不要留空槽；
- 每种可能生成的类型至少有一份配置；
- Start 和 Boss 的内容可以先只有一份；
- Normal 应至少两份，才能实际看到内容随机；
- 如果某深度没有匹配项，Reference 会回退到该类型任意非空配置并记录 Warning，避免直接生成一个意外空房；这个 Warning 仍表示资产配置需要修正。

### 6.4 特殊房为什么放叶节点

宝箱和恢复房是“主动绕路换资源”的选择。如果它们位于主路必经节点，玩家不需要承担探索成本；放在叶节点后，玩家要进去再原路返回分岔点，地图分支才有玩法意义。Boss 也放最远叶节点，保证它不会成为去其他房间的中转站。

精英房不强制是叶节点，因为它可以成为主路上的风险门槛，也可以随机落在分支中。首版只用 `MinEliteDepth` 避免它贴脸出生。

> ✅ **步骤 6 验收标准**：六种房间都能在若干 Seed 中出现；Start 为空、Boss 最远、宝箱/恢复是叶节点；深度限制生效；同 Seed 选择同一批 RoomConfig；没有“配置缺失”的 Error 或回退 Warning。

---

## 步骤 7：最小局内强化闭环

**这一步做什么**：清房奖励不再只是弹药拾取，而是出现三选一；强化只在当前 Run 生效，并复用现有 `DamageInfo`、状态异常和玩家能力边界。

首版提供七种基础强化：伤害、冷却、移动、最大生命、灼烧、冰冻、击退。它们足够证明“路线选择 → 变强 → 打 Boss”的 Roguelike 循环，不在这一步做稀有度、组合词条或装备栏。

### 7.1 新建 `Assets/Scripts/Rewards/RunUpgradeData.cs`

新建 `Rewards` 目录，命名空间使用 `Game.Rewards`。

```csharp
using UnityEngine;

namespace Game.Rewards
{
    public enum RunUpgradeType
    {
        DamagePercent,
        CooldownReduction,
        MoveSpeedPercent,
        MaxHealth,
        Burn,
        Freeze,
        KnockbackPercent,
    }

    /// <summary>一项仅在本局生效、可出现在奖励三选一中的强化配置。</summary>
    [CreateAssetMenu(fileName = "NewRunUpgrade", menuName = "Game/Run Upgrade")]
    public sealed class RunUpgradeData : ScriptableObject
    {
        [Header("显示")]
        [SerializeField] private string displayName = "Upgrade";
        [TextArea]
        [SerializeField] private string description;
        [SerializeField] private Sprite icon;

        [Header("规则")]
        [SerializeField] private RunUpgradeType type;
        [SerializeField, Min(1)] private int maxStacks = 3;
        [Tooltip("百分比填 0.2 表示 20%；最大生命填写整数；燃烧填写每跳伤害。")]
        [SerializeField] private float value = 0.2f;
        [Tooltip("燃烧/冰冻的持续时间。")]
        [SerializeField] private float duration = 3f;
        [Tooltip("燃烧的跳伤间隔。")]
        [SerializeField] private float interval = 0.5f;

        public string DisplayName => displayName;
        public string Description => description;
        public Sprite Icon => icon;
        public RunUpgradeType Type => type;
        public int MaxStacks => Mathf.Max(1, maxStacks);
        public float Value => value;
        public float Duration => duration;
        public float Interval => interval;
    }
}
```

### 7.2 新建 `Assets/Scripts/Rewards/RunModifierSet.cs`

```csharp
using Game.Core;
using Game.Entities;
using UnityEngine;

namespace Game.Rewards
{
    /// <summary>保存玩家当前一局的累计数值与元素强化。</summary>
    [RequireComponent(typeof(Health))]
    public sealed class RunModifierSet : MonoBehaviour
    {
        private Health health;
        private int bonusBurnDamage;
        private float bonusBurnDuration;
        private float bonusBurnInterval = 0.5f;
        private float bonusFreezePercent;
        private float bonusFreezeDuration;

        public float DamageMultiplier { get; private set; } = 1f;
        public float CooldownMultiplier { get; private set; } = 1f;
        public float MoveSpeedMultiplier { get; private set; } = 1f;
        public float KnockbackMultiplier { get; private set; } = 1f;
        public bool HasBonusBurn => bonusBurnDamage > 0 && bonusBurnDuration > 0f;
        public bool HasBonusFreeze => bonusFreezePercent > 0f && bonusFreezeDuration > 0f;

        public StatusEffectConfig BonusBurn =>
            StatusEffectConfig.Burn(bonusBurnDuration, bonusBurnDamage, bonusBurnInterval);
        public StatusEffectConfig BonusFreeze =>
            StatusEffectConfig.Freeze(bonusFreezeDuration, bonusFreezePercent);

        private void Awake()
        {
            health = GetComponent<Health>();
        }

        public void Apply(RunUpgradeData upgrade)
        {
            if (upgrade == null) return;
            switch (upgrade.Type)
            {
                case RunUpgradeType.DamagePercent:
                    DamageMultiplier += Mathf.Max(0f, upgrade.Value);
                    break;
                case RunUpgradeType.CooldownReduction:
                    CooldownMultiplier = Mathf.Max(0.25f, CooldownMultiplier - Mathf.Max(0f, upgrade.Value));
                    break;
                case RunUpgradeType.MoveSpeedPercent:
                    MoveSpeedMultiplier += Mathf.Max(0f, upgrade.Value);
                    break;
                case RunUpgradeType.MaxHealth:
                    health.IncreaseMaxHealth(Mathf.Max(1, Mathf.RoundToInt(upgrade.Value)), true);
                    break;
                case RunUpgradeType.Burn:
                    bonusBurnDamage += Mathf.Max(1, Mathf.RoundToInt(upgrade.Value));
                    bonusBurnDuration = Mathf.Max(bonusBurnDuration, upgrade.Duration);
                    bonusBurnInterval = upgrade.Interval > 0f ? upgrade.Interval : 0.5f;
                    break;
                case RunUpgradeType.Freeze:
                    bonusFreezePercent = Mathf.Clamp(bonusFreezePercent + upgrade.Value, 0f, 0.8f);
                    bonusFreezeDuration = Mathf.Max(bonusFreezeDuration, upgrade.Duration);
                    break;
                case RunUpgradeType.KnockbackPercent:
                    KnockbackMultiplier += Mathf.Max(0f, upgrade.Value);
                    break;
            }
        }
    }
}
```

`RunModifierSet` 挂在 Player 上，是一局累计数值的唯一来源。它不读取输入、不发射武器，也不显示 UI。

冷却缩减内部保存的是倍率：初始 `1`，每次 `-0.1`，最低 `0.25`。这样可以避免“减 100% 后冷却为负数”。

### 7.3 新建 `Assets/Scripts/Rewards/RunUpgradeManager.cs`

```csharp
using System;
using System.Collections.Generic;
using Game.Commands;
using Game.Core;
using Game.Debug;
using Game.UI;
using UnityEngine;

namespace Game.Rewards
{
    /// <summary>从仍可叠加的强化中抽取三选一，并把选择应用到玩家。</summary>
    [RequireComponent(typeof(RunModifierSet))]
    public sealed class RunUpgradeManager : MonoBehaviour
    {
        [SerializeField] private RunUpgradeData[] upgradePool;
        [SerializeField] private RewardChoiceUI rewardChoiceUI;

        private readonly Dictionary<RunUpgradeData, int> stackCounts =
            new Dictionary<RunUpgradeData, int>();
        private RunModifierSet modifiers;
        private PlayerInputHandler inputHandler;
        private System.Random random;
        private Action pendingCompletion;
        private bool presenting;

        private void Awake()
        {
            modifiers = GetComponent<RunModifierSet>();
            inputHandler = GetComponent<PlayerInputHandler>();
        }

        private void Start()
        {
            int seed = RunManager.Instance != null ? RunManager.Instance.CurrentSeed : 0;
            random = new System.Random(unchecked(seed ^ 0x5F3759DF));
        }

        public void PresentChoices(Action onCompleted)
        {
            if (presenting)
            {
                GameDebug.Error(DebugCategory.Level, "奖励 UI 已经处于选择中，拒绝重复打开。", this);
                return;
            }

            List<RunUpgradeData> eligible = CollectEligibleUpgrades();
            if (eligible.Count == 0)
            {
                GameDebug.Warning(DebugCategory.Level, "没有仍可获取的局内强化，房间直接解锁。", this);
                onCompleted?.Invoke();
                return;
            }
            if (rewardChoiceUI == null)
            {
                GameDebug.Error(DebugCategory.Level, "RunUpgradeManager 没有连接 RewardChoiceUI。", this);
                onCompleted?.Invoke();
                return;
            }

            Shuffle(eligible);
            int choiceCount = Mathf.Min(3, eligible.Count);
            List<RunUpgradeData> choices = eligible.GetRange(0, choiceCount);
            pendingCompletion = onCompleted;
            presenting = true;
            if (inputHandler != null) inputHandler.enabled = false;
            rewardChoiceUI.Show(choices, OnUpgradeChosen);
        }

        private List<RunUpgradeData> CollectEligibleUpgrades()
        {
            List<RunUpgradeData> result = new List<RunUpgradeData>();
            if (upgradePool == null) return result;
            for (int i = 0; i < upgradePool.Length; i++)
            {
                RunUpgradeData upgrade = upgradePool[i];
                if (upgrade == null) continue;
                int stacks = stackCounts.TryGetValue(upgrade, out int count) ? count : 0;
                if (stacks < upgrade.MaxStacks) result.Add(upgrade);
            }
            return result;
        }

        private void OnUpgradeChosen(RunUpgradeData upgrade)
        {
            if (!presenting || upgrade == null) return;
            modifiers.Apply(upgrade);
            stackCounts[upgrade] = stackCounts.TryGetValue(upgrade, out int count) ? count + 1 : 1;
            EventBus.Publish(new RunUpgradeSelectedEvent(upgrade.DisplayName));
            GameDebug.Log(DebugCategory.Combat, $"获得局内强化：{upgrade.DisplayName}。", this);

            rewardChoiceUI.Hide();
            if (inputHandler != null) inputHandler.enabled = true;
            presenting = false;
            Action completion = pendingCompletion;
            pendingCompletion = null;
            completion?.Invoke();
        }

        private void Shuffle<T>(IList<T> list)
        {
            if (random == null) random = new System.Random(0);
            for (int i = list.Count - 1; i > 0; i--)
            {
                int j = random.Next(i + 1);
                T temp = list[i];
                list[i] = list[j];
                list[j] = temp;
            }
        }
    }
}
```

Manager 负责：

- 根据 `maxStacks` 排除已满强化；
- 使用独立的 Seed 随机流洗牌；
- 最多取三项交给 UI；
- 选择期间禁用 `PlayerInputHandler`；
- 应用强化、广播统计事件，再通知 Room 开门。

如果强化池全部叠满，房间会直接完成奖励并开门，不能把玩家永久锁住。

### 7.4 小范围更新 `PlayerInputHandler.cs`

奖励选择和最终结算都会临时禁用输入组件。仅仅 `enabled = false` 不会自动清除 `PlayerController` 中上一帧的持续移动方向，也不会清空尚未执行的离散命令；玩家可能继续走，或者重新启用后立刻执行选择前缓存的闪避/手雷。

在 `PlayerInputHandler` 中加入完整生命周期方法：

```csharp
private void OnDisable()
{
    if (player != null) player.SetMoveInput(Vector2.zero);
    buffer?.Clear();
}
```

这仍然遵守“输入只在 PlayerInputHandler 收口”的边界：外部系统只开关组件，不需要分别认识移动命令和输入缓冲。

### 7.5 新建 `Assets/Scripts/UI/RewardChoiceUI.cs`

```csharp
using System;
using System.Collections.Generic;
using Game.Rewards;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

namespace Game.UI
{
    /// <summary>使用固定槽位显示一次局内强化选择。</summary>
    public sealed class RewardChoiceUI : MonoBehaviour
    {
        [Serializable]
        private sealed class ChoiceSlot
        {
            public Button button = null;
            public Image icon = null;
            public TMP_Text title = null;
            public TMP_Text description = null;
        }

        [SerializeField] private GameObject panel;
        [SerializeField] private ChoiceSlot[] slots;

        private readonly List<RunUpgradeData> currentChoices = new List<RunUpgradeData>();
        private Action<RunUpgradeData> onSelected;

        private void Awake()
        {
            if (panel != null) panel.SetActive(false);
        }

        public void Show(IReadOnlyList<RunUpgradeData> choices, Action<RunUpgradeData> selectionCallback)
        {
            currentChoices.Clear();
            for (int i = 0; i < choices.Count; i++) currentChoices.Add(choices[i]);
            onSelected = selectionCallback;

            for (int i = 0; i < slots.Length; i++)
            {
                ChoiceSlot slot = slots[i];
                bool active = i < currentChoices.Count;
                if (slot.button == null) continue;
                slot.button.gameObject.SetActive(active);
                slot.button.onClick.RemoveAllListeners();
                if (!active) continue;

                int capturedIndex = i;
                RunUpgradeData upgrade = currentChoices[i];
                if (slot.icon != null)
                {
                    slot.icon.sprite = upgrade.Icon;
                    slot.icon.enabled = upgrade.Icon != null;
                }
                if (slot.title != null) slot.title.text = upgrade.DisplayName;
                if (slot.description != null) slot.description.text = upgrade.Description;
                slot.button.onClick.AddListener(() => Select(capturedIndex));
            }

            if (panel != null) panel.SetActive(true);
        }

        public void Hide()
        {
            if (panel != null) panel.SetActive(false);
            onSelected = null;
            currentChoices.Clear();
        }

        private void Select(int index)
        {
            if (index < 0 || index >= currentChoices.Count) return;
            Action<RunUpgradeData> callback = onSelected;
            onSelected = null;
            callback?.Invoke(currentChoices[index]);
        }
    }
}
```

三个 Button 的监听由脚本每次 Show 时动态绑定，Inspector 中不要再手动填写 OnClick，否则一次点击可能执行两次。

### 7.6 小范围更新 `Health.cs`

在 `Health` 中加入下面两个完整方法。`Heal` 保持通用，只更新本地 `HealthChanged`；玩家的全局血条事件仍由 `PlayerController` 桥接。

```csharp
public void Heal(int amount)
{
    if (isDead || amount <= 0) return;
    int previous = Current;
    Current = Mathf.Min(maxHealth, Current + amount);
    if (Current != previous) HealthChanged?.Invoke(Current, maxHealth);
}

public void IncreaseMaxHealth(int amount, bool healByAmount)
{
    if (amount <= 0) return;
    maxHealth += amount;
    if (healByAmount) Current = Mathf.Min(maxHealth, Current + amount);
    HealthChanged?.Invoke(Current, maxHealth);
}
```

### 7.7 小范围更新 `PlayerController.cs`

添加命名空间和只读组件属性：

```csharp
using Game.Rewards;

public RunModifierSet RunModifiers { get; private set; }
```

在 `Awake()` 获取其他组件的位置加入完整语句：

```csharp
RunModifiers = GetComponent<RunModifierSet>();
```

把原 `MoveSpeed` 属性完整替换为：

```csharp
public float MoveSpeed => moveSpeed * (RunModifiers != null ? RunModifiers.MoveSpeedMultiplier : 1f);
```

`RunModifierSet` 与 PlayerController 在同一 Player 物体上，因此直接 `GetComponent`，不增加 Inspector 引用。不在 `PlayerController.Update` 里计算 Buff，也不让状态类认识强化系统。

### 7.8 小范围更新 `WeaponController.cs`

添加命名空间和私有缓存：

```csharp
using Game.Rewards;

private RunModifierSet runModifiers;
```

在 `Awake()` 获取 `playerController` 后加入：

```csharp
runModifiers = GetComponent<RunModifierSet>();
```

开火成功后设置冷却的完整语句改成：

```csharp
float cooldownMultiplier = runModifiers != null ? runModifiers.CooldownMultiplier : 1f;
cooldownTimer = data.cooldown * cooldownMultiplier;
```

把 `BuildDamageInfo()` 完整替换为 Reference 中的最终版本：

```csharp
private DamageInfo BuildDamageInfo(WeaponData data)
{
    float damageMultiplier = runModifiers != null ? runModifiers.DamageMultiplier : 1f;
    float knockbackMultiplier = runModifiers != null ? runModifiers.KnockbackMultiplier : 1f;
    int damage = Mathf.Max(1, Mathf.RoundToInt(data.damage * damageMultiplier));
    DamageInfo info = new DamageInfo(damage, data.knockbackForce * knockbackMultiplier);
    if (data.burnDamagePerTick > 0 && data.burnDuration > 0f)
    {
        info = info.WithAddedStatus(StatusEffectConfig.Burn(
            data.burnDuration, data.burnDamagePerTick, data.burnTickInterval));
    }
    if (data.freezePercent > 0f && data.freezeDuration > 0f)
    {
        info = info.WithAddedStatus(StatusEffectConfig.Freeze(
            data.freezeDuration, data.freezePercent));
    }
    if (runModifiers != null && runModifiers.HasBonusBurn)
        info = info.WithAddedStatus(runModifiers.BonusBurn);
    if (runModifiers != null && runModifiers.HasBonusFreeze)
        info = info.WithAddedStatus(runModifiers.BonusFreeze);
    return info;
}
```

武器自带元素与局内元素会各自通过 `WithAddedStatus` 加入同一个 `DamageInfo`；最终如何合并同类状态，继续由现有 `StatusEffectManager` 的规则统一决定。不要让 Bullet 或每个 Strategy 各算一遍强化。

### 7.9 创建七份强化资产

`Create > Game > Run Upgrade`，建议初始值：

| 名称 | Type | Value | Duration | Interval | Max Stacks |
|---|---|---:|---:|---:|---:|
| 强力弹头 | DamagePercent | 0.20 | - | - | 5 |
| 快速装填 | CooldownReduction | 0.10 | - | - | 5 |
| 轻盈步伐 | MoveSpeedPercent | 0.15 | - | - | 4 |
| 生命扩容 | MaxHealth | 20 | - | - | 5 |
| 灼热弹药 | Burn | 2 | 3 | 0.5 | 3 |
| 冰霜弹药 | Freeze | 0.15 | 2.5 | - | 3 |
| 冲击弹头 | KnockbackPercent | 0.25 | - | - | 4 |

描述文字直接写清精确数值，方便验收，不要先写含糊的“略微提高”。

### 7.10 UI 与 Player 配置

1. Canvas 下创建 `RewardPanel`，放三个同结构 Choice Button；每个包含 Icon、Title、Description。
2. `RewardChoiceUI` 仍挂在始终激活的 Canvas 根物体，Panel 自身由它开关。
3. Slots 数组大小设为 3，逐项连接四个字段。
4. Player 添加 `RunModifierSet` 和 `RunUpgradeManager`。
5. `RunUpgradeManager.upgradePool` 放入七份资产，连接 `RewardChoiceUI`。
6. `PlayerController` 与 `WeaponController` 会自行 `GetComponent<RunModifierSet>()`，确认三个组件确实挂在同一个 Player 物体上。
7. `LevelManager.upgradeManager` 指向 Player 上的 `RunUpgradeManager`。

> ✅ **步骤 7 验收标准**：清空有奖励的房间后三选一出现且门保持锁定；点击后 UI 消失、门打开、输入恢复；相同 Seed 和相同路线的候选顺序一致；七种强化都能通过数值或战斗表现验证；重开场景后所有叠层归零。

---

## 步骤 8：二维小地图与最终整体验收

**这一步做什么**：把旧的一排房间格子升级为真实二维拓扑，显示连接、探索迷雾、房间类型、当前房和清空状态。

### 8.1 大规模重写 `Assets/Scripts/UI/MinimapUI.cs`

```csharp
using System.Collections.Generic;
using Game.Core;
using UnityEngine;
using UnityEngine.UI;

namespace Game.UI
{
    /// <summary>根据 RoomMapData 的网格坐标绘制房间、连线、探索与清空状态。</summary>
    public sealed class MinimapUI : MonoBehaviour
    {
        private sealed class ConnectionVisual
        {
            public Image Image;
            public int A;
            public int B;
        }

        [SerializeField] private RectTransform connectionContainer;
        [SerializeField] private RectTransform iconContainer;
        [SerializeField] private Image iconPrefab;
        [SerializeField] private Image connectionPrefab;
        [SerializeField] private Vector2 cellSize = new Vector2(48f, 48f);
        [SerializeField] private float connectionThickness = 6f;

        [Header("颜色")]
        [SerializeField] private Color currentColor = new Color(1f, 0.9f, 0.2f);
        [SerializeField] private Color clearedColor = new Color(0.3f, 0.7f, 0.4f);
        [SerializeField] private Color normalColor = new Color(0.55f, 0.55f, 0.6f);
        [SerializeField] private Color eliteColor = new Color(0.75f, 0.3f, 0.85f);
        [SerializeField] private Color treasureColor = new Color(1f, 0.65f, 0.1f);
        [SerializeField] private Color recoveryColor = new Color(0.2f, 0.8f, 0.75f);
        [SerializeField] private Color bossColor = new Color(0.85f, 0.25f, 0.25f);
        [SerializeField] private Color connectionColor = new Color(0.45f, 0.45f, 0.5f);

        private readonly Dictionary<int, RoomMapData> roomById = new Dictionary<int, RoomMapData>();
        private readonly Dictionary<int, Image> iconById = new Dictionary<int, Image>();
        private readonly HashSet<int> discovered = new HashSet<int>();
        private readonly HashSet<int> cleared = new HashSet<int>();
        private readonly List<ConnectionVisual> connections = new List<ConnectionVisual>();
        private int currentRoomId = -1;
        private Vector2 mapOffset;

        private void OnEnable()
        {
            EventBus.Subscribe<LevelStartedEvent>(OnLevelStarted);
            EventBus.Subscribe<RoomEnteredEvent>(OnRoomEntered);
            EventBus.Subscribe<RoomClearedEvent>(OnRoomCleared);
            EventBus.Subscribe<RunEndedEvent>(OnRunEnded);
        }

        private void OnDisable()
        {
            EventBus.Unsubscribe<LevelStartedEvent>(OnLevelStarted);
            EventBus.Unsubscribe<RoomEnteredEvent>(OnRoomEntered);
            EventBus.Unsubscribe<RoomClearedEvent>(OnRoomCleared);
            EventBus.Unsubscribe<RunEndedEvent>(OnRunEnded);
        }

        private void OnLevelStarted(LevelStartedEvent e)
        {
            ClearVisuals();
            if (e.Rooms == null || e.Rooms.Length == 0) return;

            CalculateMapOffset(e.Rooms);
            for (int i = 0; i < e.Rooms.Length; i++) roomById[e.Rooms[i].Id] = e.Rooms[i];
            CreateConnections(e.Rooms);
            CreateIcons(e.Rooms);

            for (int i = 0; i < e.Rooms.Length; i++)
                if (e.Rooms[i].Type == RoomType.Start) discovered.Add(e.Rooms[i].Id);
            Refresh();
        }

        private void OnRoomEntered(RoomEnteredEvent e)
        {
            currentRoomId = e.RoomId;
            Discover(e.RoomId);
            if (roomById.TryGetValue(e.RoomId, out RoomMapData room))
            {
                Discover(room.NorthId);
                Discover(room.EastId);
                Discover(room.SouthId);
                Discover(room.WestId);
            }
            Refresh();
        }

        private void OnRoomCleared(RoomClearedEvent e)
        {
            cleared.Add(e.RoomId);
            Refresh();
        }

        private void OnRunEnded(RunEndedEvent e)
        {
            currentRoomId = -1;
            Refresh();
        }

        private void CreateIcons(IReadOnlyList<RoomMapData> rooms)
        {
            for (int i = 0; i < rooms.Count; i++)
            {
                RoomMapData room = rooms[i];
                Image icon = Instantiate(iconPrefab, iconContainer);
                icon.name = $"RoomIcon_{room.Id}_{room.Type}";
                ((RectTransform)icon.transform).anchoredPosition = ToUiPosition(room.GridPosition);
                iconById.Add(room.Id, icon);
            }
        }

        private void CreateConnections(IReadOnlyList<RoomMapData> rooms)
        {
            for (int i = 0; i < rooms.Count; i++)
            {
                RoomMapData room = rooms[i];
                if (room.EastId >= 0) CreateConnection(room.Id, room.EastId);
                if (room.NorthId >= 0) CreateConnection(room.Id, room.NorthId);
            }
        }

        private void CreateConnection(int a, int b)
        {
            if (!roomById.TryGetValue(a, out RoomMapData roomA)
                || !roomById.TryGetValue(b, out RoomMapData roomB)) return;

            Vector2 start = ToUiPosition(roomA.GridPosition);
            Vector2 end = ToUiPosition(roomB.GridPosition);
            Vector2 delta = end - start;
            Image image = Instantiate(connectionPrefab, connectionContainer);
            RectTransform rect = (RectTransform)image.transform;
            rect.anchoredPosition = (start + end) * 0.5f;
            rect.sizeDelta = new Vector2(delta.magnitude, connectionThickness);
            rect.localRotation = Quaternion.Euler(0f, 0f, Mathf.Atan2(delta.y, delta.x) * Mathf.Rad2Deg);
            image.color = connectionColor;
            connections.Add(new ConnectionVisual { Image = image, A = a, B = b });
        }

        private void Refresh()
        {
            foreach (KeyValuePair<int, Image> pair in iconById)
            {
                bool visible = discovered.Contains(pair.Key);
                pair.Value.gameObject.SetActive(visible);
                if (visible) pair.Value.color = GetRoomColor(pair.Key);
            }

            for (int i = 0; i < connections.Count; i++)
            {
                ConnectionVisual connection = connections[i];
                connection.Image.gameObject.SetActive(
                    discovered.Contains(connection.A) && discovered.Contains(connection.B));
            }
        }

        private Color GetRoomColor(int roomId)
        {
            if (roomId == currentRoomId) return currentColor;
            if (cleared.Contains(roomId)) return clearedColor;
            if (!roomById.TryGetValue(roomId, out RoomMapData room)) return normalColor;
            switch (room.Type)
            {
                case RoomType.Elite: return eliteColor;
                case RoomType.Treasure: return treasureColor;
                case RoomType.Recovery: return recoveryColor;
                case RoomType.Boss: return bossColor;
                default: return normalColor;
            }
        }

        private void CalculateMapOffset(IReadOnlyList<RoomMapData> rooms)
        {
            Vector2Int min = rooms[0].GridPosition;
            Vector2Int max = rooms[0].GridPosition;
            for (int i = 1; i < rooms.Count; i++)
            {
                min = Vector2Int.Min(min, rooms[i].GridPosition);
                max = Vector2Int.Max(max, rooms[i].GridPosition);
            }
            Vector2 center = ((Vector2)min + (Vector2)max) * 0.5f;
            mapOffset = new Vector2(-center.x * cellSize.x, -center.y * cellSize.y);
        }

        private Vector2 ToUiPosition(Vector2Int gridPosition)
        {
            return new Vector2(gridPosition.x * cellSize.x, gridPosition.y * cellSize.y) + mapOffset;
        }

        private void Discover(int roomId)
        {
            if (roomId >= 0) discovered.Add(roomId);
        }

        private void ClearVisuals()
        {
            DestroyChildren(iconContainer);
            DestroyChildren(connectionContainer);
            roomById.Clear();
            iconById.Clear();
            discovered.Clear();
            cleared.Clear();
            connections.Clear();
            currentRoomId = -1;
        }

        private static void DestroyChildren(Transform parent)
        {
            if (parent == null) return;
            for (int i = parent.childCount - 1; i >= 0; i--)
                Destroy(parent.GetChild(i).gameObject);
        }
    }
}
```

小地图仍然只订阅 EventBus，不引用 `LevelManager`、`Room` 或 GameObject。`RoomMapData` 是它唯一需要的地图协议。

迷雾规则是：

- 开局只发现 Start；
- 进入一间房后，发现当前房和它的直接邻居；
- 未发现房间和连接完全隐藏；
- 当前房黄色优先级最高，清空房绿色次之，未清特殊房使用类型色；
- Run 结束后取消当前高亮，但保留已探索地图作为结算背景。

### 8.2 小地图 Prefab 和层级

```text
MinimapRoot (MinimapUI)
├── ConnectionContainer     # 先绘制，层级在图标后面
└── IconContainer           # 后绘制，显示在连线上面
```

创建两个简单 Image Prefab：

- `RoomIcon2D.prefab`：例如 `28 × 20` 的白色矩形，颜色由脚本改；
- `RoomConnection2D.prefab`：白色 Image，Pivot `(0.5, 0.5)`，脚本会设置长度和旋转。

两个 Container 的 Anchor/Pivot 建议都设中心 `(0.5, 0.5)`。初始 `cellSize = (48,48)`；如果地图超出面板，先减到 `(36,36)`，不要在这一阶段引入缩放和拖拽系统。

### 8.3 删除开发预览组件

`LevelGraphDebugView.cs` 可以保留在代码库中作为开发工具，但正式场景不需要挂它。批量 Seed 验证随时可以在独立测试场景执行。

### 8.4 完整一局最终验收

使用固定 Seed 完成一次，然后使用新 Seed 再完成或失败一次。

**生成与空间**

- [ ] 房间数在设置范围内；地图至少有一个三岔节点。
- [ ] Start、Treasure、Recovery、Elite、Boss 数量正确。
- [ ] Boss 是足够深的叶节点，宝箱与恢复也是叶节点。
- [ ] 所有小地图边都对应一条真实可走通道，反之亦然。
- [ ] 没有重叠房间、断路、墙洞或能绕出地图的缺口。
- [ ] 同 Seed 重开后拓扑、内容配置和初始奖励候选一致。

**房间流程**

- [ ] 首次进入战斗房才生成敌人并封锁所有连接门。
- [ ] 最后一名敌人死亡才结束战斗。
- [ ] 有奖励时选完才开门；空奖励池也不会锁死。
- [ ] 重访房间不刷怪、不重复奖励、不重复恢复。
- [ ] 可以自由回到分岔点，再选择另一条路线。

**镜头与 UI**

- [ ] 房内固定镜头、通道跟随镜头切换稳定。
- [ ] 小地图方向与世界一致，连接不重复。
- [ ] 当前/清空/精英/宝箱/恢复/Boss 颜色正确。
- [ ] 未发现房间不会提前泄露，但相邻可探索房会显现。

**单局闭环**

- [ ] 强化实际改变伤害、冷却、移动、血量或状态效果。
- [ ] 玩家死亡显示失败，Boss 死亡显示胜利。
- [ ] 结算统计中的房间、敌人和强化数量合理。
- [ ] 同 Seed 重开与新 Seed 重开都恢复时间、输入和局内状态。
- [ ] DebugOverlay 没有持续刷屏的 Error/Warning。

> ✅ **步骤 8 / V1.5-3 验收标准**：从生成地图开始，能够探索分支、清房、获取强化、回访、击败 Boss 并结算；死亡也能正常结算；同 Seed 可以稳定复现；以上清单全部通过后，才把 V1.5-3 记为完成。

---

## 最终文件变更总表

### 新建 17 个脚本

```text
Assets/Scripts/Core/RunManager.cs
Assets/Scripts/Debug/Debugging/LevelGraphDebugView.cs
Assets/Scripts/Level/Generation/RoomDirectionUtility.cs
Assets/Scripts/Level/Generation/RoomNode.cs
Assets/Scripts/Level/Generation/LevelGraph.cs
Assets/Scripts/Level/Generation/LevelGenerationSettings.cs
Assets/Scripts/Level/Generation/LevelGraphGenerator.cs
Assets/Scripts/Level/Generation/LevelGraphValidator.cs
Assets/Scripts/Level/Building/RoomCatalog.cs
Assets/Scripts/Level/Rooms/RoomConnection.cs
Assets/Scripts/Level/Rooms/RoomEncounterTrigger.cs
Assets/Scripts/Level/Building/DungeonBuilder.cs
Assets/Scripts/Rewards/RunUpgradeData.cs
Assets/Scripts/Rewards/RunModifierSet.cs
Assets/Scripts/Rewards/RunUpgradeManager.cs
Assets/Scripts/UI/RewardChoiceUI.cs
Assets/Scripts/UI/RunResultUI.cs
```

V1.5-3 完成后总脚本数从当前 89 增至 105，因为删除 `VictoryUI.cs` 抵消一个新增文件：`89 + 17 - 1 = 105`。

### 大规模重写 4 个脚本

```text
Assets/Scripts/Level/Rooms/Room.cs
Assets/Scripts/Level/LevelManager.cs
Assets/Scripts/Level/Rooms/Door.cs
Assets/Scripts/UI/MinimapUI.cs
```

### 小范围扩展 6 个脚本

```text
Assets/Scripts/Core/GameEvents.cs
Assets/Scripts/Commands/PlayerInputHandler.cs
Assets/Scripts/Entities/Health.cs
Assets/Scripts/Entities/PlayerController.cs
Assets/Scripts/Weapons/WeaponController.cs
Assets/Scripts/Level/Rooms/RoomConfig.cs
```

### 删除 1 个脚本

```text
Assets/Scripts/UI/VictoryUI.cs
```

只在 `RunResultUI` 已通过胜利与失败验收、场景旧组件已经移除后再删除。

### 新建的主要资产与 Prefab

```text
Assets/Data/Rooms/LevelGenerationSettings.asset
Assets/Data/RoomCatalog.asset
Assets/Data/Rooms/...               # 六类 RoomConfig，普通房至少两份
Assets/Data/RunUpgrades/...         # 七份 RunUpgradeData
Assets/Prefabs/Level/GeneratedRoom.prefab
Assets/Prefabs/Level/HorizontalCorridor.prefab
Assets/Prefabs/Level/VerticalCorridor.prefab
Assets/Prefabs/UI/RoomIcon2D.prefab
Assets/Prefabs/UI/RoomConnection2D.prefab
```

目录名可按当前项目习惯调整；代码中的引用全部通过 Inspector，不依赖这些字符串路径。

---

## 常见问题与提前排雷

### 1. 为什么不随机拼不同尺寸房间？

那需要连接口匹配、占地栅格、旋转规则、通道长度和碰撞烘焙一起升级。首版先让所有房间共享统一几何尺寸，把随机性放在拓扑和内容上；系统边界稳定后再增加模板形状。

### 2. 为什么不把每间房做成独立 Scene？

当前规模只有 9～12 个灰盒房，单场景实例化更直观，也便于自由回头。Additive Scene 更适合大型房间的异步加载；现在使用会先引入生命周期复杂度，却没有实际性能收益。

### 3. 房间清完，回通道后门又锁了

检查 `Room.HandlePlayerEntered()`：`if (spawned) return;` 必须在 Spawn/Lock 之前。重访不能重复执行首次进入流程。

### 4. 玩家进入空房后被永久关住

`SpawnContents()` 后必须根据 `aliveCount` 分支：大于 0 才封门，否则立即 `CompleteEncounter()`。这是第 5 周已经踩过的“空房兜底”在新结构中的对应版本。

### 5. 两个相邻房间之间出现双通道

Builder 只能为 East 和 North 创建实体通道；如果四向全创建，每条无向边会出现两份重叠实例。

### 6. 通道对不上门洞

Reference 不自动拉伸 Corridor。核对房间中心间距、门口 Anchor 和 Corridor Prefab 的长度；不要通过给整个 Corridor 非等比 Scale 临时遮住，因为 Collider 厚度也会一起变形。

### 7. 进入通道镜头停在旧房间

检查 EncounterTrigger 是否覆盖到了通道，或 `OnTriggerExit2D` 是否因为 Player Collider/Tag 不正确而没有触发。先在 DebugOverlay 观察 Room 进入事件，再判断是触发器还是 Cinemachine 配置。

### 8. 同 Seed 地图相同，但奖励不同

必须选择相同路线并清相同数量的奖励房。奖励随机流是确定的，但“第几次抽奖”也是状态的一部分。若完全相同的操作仍不同，再检查是否有代码使用了 `UnityEngine.Random` 或额外消耗了奖励 `System.Random`。

### 9. 选择强化后玩家仍不能移动

检查 `RunUpgradeManager.OnUpgradeChosen()` 是否重新启用 `PlayerInputHandler`，以及 Run 是否已经结算。输入的开关只应该由奖励选择与最终结算控制，不要禁用整个 Player GameObject。

### 10. 最大生命增加了，血条最大值没变

`Health.IncreaseMaxHealth()` 必须调用 `HealthChanged(current,max)`。玩家的 `PlayerController` 已订阅这个本地事件并桥接到全局血条事件，无需让 `RunModifierSet` 直接认识 UI。

### 11. Boss 死亡后同时弹出两套胜利 UI

场景里仍存在旧 `VictoryUI`。禁用并移除旧组件，确认 `RunResultUI` 胜负都正常后再删除旧脚本。

### 12. 生成失败时不要继续实例化“半张图”

`TryGenerate` 失败后 LevelManager 应记录 Error、禁用自身并直接返回。不要在 graph 为 null 时让 Builder 猜测修复；生成失败必须留在纯数据边界解决。

---

## 可选课后练习

这些练习不属于 V1.5-3 验收硬要求，建议主流程稳定后再做。

### 练习 1：给生成器增加环路

在树完成后，从相邻但尚未连接的节点对中额外抽 1 条边。思考并验证：

- 边数不再是 `N - 1` 后，Validator 应如何表达“最多一个环”？
- 小地图和实体通道是否无需修改就能支持？
- Depth 应继续沿生成父子关系，还是改成从 Start 做 BFS 的最短距离？

### 练习 2：未探索特殊房不提前暴露类型

当前相邻房被发现时会直接显示类型色。把规则改为：相邻房只显示灰色问号，真正进入后才显示精英/宝箱/恢复/Boss 类型。不要让 Minimap 引用 Room；应扩展事件或本地发现状态。

### 练习 3：为房间配置增加“避免连续重复”

同类型抽取时，避免连续两个房间选到同一份 `RoomConfig`。注意不能用全局最近一次配置，因为不同分支的建造顺序不等于玩家行走顺序；先决定你要避免的是“生成顺序重复”“图上相邻重复”还是“游玩顺序重复”。

### 练习 4：验证坏 Prefab，而不是等 Play 时撞墙

为 `Room` 增加 Editor 期或运行时一次性检查：四个 Connection 是否齐全、方向是否重复、Trigger 是否连接、每个 Gate 是否有 Collider。把所有问题一次列出，不要发现第一个就 return。

---

## 设计决策回顾

1. **先图后实体**：拓扑错误在纯数据层发现，不让 Unity 场景替算法兜底。
2. **首版树结构**：足以表达路线选择，同时规避交叉和环路复杂度。
3. **门是物理门，不是传送器**：导航由真实空间完成，Door 只负责封锁。
4. **RoomConfig 不决定拓扑**：它描述房间内容；RoomCatalog 根据节点类型与深度选内容。
5. **局部事件与全局事件继续分层**：Room 用 C# 事件通知 LevelManager；LevelManager 再桥接成 UI 可订阅的 EventBus 事件。
6. **Seed 随机流隔离**：拓扑、内容、奖励互不消耗对方的随机序列。
7. **恢复房代替假商店**：只实现已有玩法资源能支撑的房型。
8. **奖励先做基础数值闭环**：证明局内成长，再谈稀有度、协同和构筑流派。
9. **相机采用混合区域方案**：房内保留固定构图，通道保证连续移动。
10. **结算属于 Run，不属于 Victory UI**：同一协议覆盖胜利和失败，并提供可复现重开。

---

## 当前实际完成记录（2026-08-19）

本节记录“已经发生的事”，与上面的实施教程分开：

- 已完成 V1.5-3 的完整架构设计与八步实施教程。
- 已把未来完成态的完整参考代码写入 `Reference/Scripts/`：Reference 当前共 105 个 `.cs` 文件。
- 准备阶段 review 发现并修正一个输入残留风险：奖励/结算禁用 `PlayerInputHandler` 时，现在会清零持续移动方向并清空输入缓冲。
- 已用 Reference 的 105 个脚本临时替换 Unity C# 工程的编译输入执行静态验证，结果为 **0 warning / 0 error**；验证结束后已恢复 `Assembly-CSharp.csproj`。
- `Assets/Scripts/`、场景、Prefab 和 ScriptableObject **尚未开始实施 V1.5-3**，当前可运行版本仍是 V1.5-debug 完成后的固定三房流程。
- 下一步应从“步骤 1：Run 闭环”开始，由用户按教程同步到 `Assets/` 并逐步 Play 验收。
- 在八步全部完成并通过最终清单前，不创建 `v1.5-3` 标签，也不把 README 状态写成已完成。

### 步骤 1 实际完成记录（2026-08-22）

- 新增 `Assets/Scripts/Core/RunManager.cs`：确定本局 Seed，统计探索房间、击败敌人与获得强化，监听玩家死亡，并统一发布胜利或失败结算。
- 新增 `Assets/Scripts/UI/RunResultUI.cs` 与结算面板：显示胜负和最小统计，提供同 Seed/新 Seed 两种场景重载入口；旧 `VictoryUI` 及其场景对象已删除。
- 旧固定三房 `LevelManager` 的数组越界通关已接入 `RunManager.CompleteRun()`；找不到 `RunManager` 时记录明确 Error，不再伪装成可用的旧结算兜底。
- 提前同步了最终版关卡事件协议：`LevelStartedEvent` 携带 `RoomMapData[]`，房间进入/清空事件使用 `RoomId`；旧 `LevelManager` 和 `MinimapUI` 已完成临时适配，`DoorEnteredEvent` 暂时保留到步骤 3 重构实体通道时删除。
- `PlayerInputHandler.OnDisable()` 会清零持续移动并清空输入缓冲。输入组件只负责读取与清理；一局结束由 `RunManager` 禁用，后续奖励流程则在奖励面板开启/关闭时临时禁用和恢复。
- `RoomType` 扩展后，三份普通房配置已在 Inspector 重新保存为 `Normal`，Boss 配置重新保存为 `Boss`，避免旧序列化整数被解释成新枚举成员。
- 代码 review 修正了 `RunUpgradeSelectedEvent.UpgradeName` 拼写，并确认 `RunManager`、`RunResultUI`、两个重开按钮及场景引用完整，旧 `VictoryUI` GUID 无残留。
- 用户已完成胜利、失败与重开的 Play 验收；静态编译结果为 **0 error / 0 项目代码 warning**（其余 warning 均来自 Unity Package）。
- 步骤 1 完成后 `Assets/Scripts/` 共 90 个 `.cs` 文件；下一步进入步骤 2，只生成并批量验证纯数据地图，暂不实例化房间。

### 步骤 2 实际完成记录（2026-08-24）

- 新增 `Assets/Scripts/Level/Generation/` 下六个脚本：`RoomDirectionUtility`、`RoomNode`、`LevelGraph`、`LevelGenerationSettings`、`LevelGraphGenerator`、`LevelGraphValidator`。当前生成器使用独立 `System.Random(seed)` 在二维网格上生成 9～12 间房的无环树结构，并为节点分配稳定 Id、坐标、深度、四向邻接和房间类型。
- 新增 `Assets/Data/Rooms/LevelGenerationSettings.asset`：当前配置要求 Boss 最小深度 4、至少一个三岔房，并生成一间精英房、一间宝箱房和一间恢复房；生成失败会换用确定性的 attempt seed 重试，最多 50 次。
- 新增 `Assets/Scripts/Debug/Debugging/LevelGraphDebugView.cs`，使用 `Game.Debugging` 命名空间，与 `Game.Debug` 日志基础设施分离。它支持指定 Seed 的 Scene Gizmo 预览、稳定签名输出和连续 Seed 批量验证。
- 用户已在 Play 模式确认纯数据地图能够预览；同一 `Preview Seed` 多次生成的签名完全一致；`Validate Seed Range` 连续验证 100 个 Seed 全部通过。旧固定三房仍正常运行，步骤 2 没有实例化任何程序化房间 GameObject。
- 验收后代码 review 抓到一个不会令验证失败的范围沉默错误：`System.Random.Next(min,max)` 不包含上界，导致配置 9～12 实际永远取不到 12。现已把目标房间数改为 `Next(MinRoomCount, MaxRoomCount + 1)`，并让 `MaxRoomCount` getter 至少返回 `minRoomCount`。
- 长地图签名还暴露了 Debug Overlay 的显示问题：缓存限制按“消息条数”计算，但 TMP 会把一条长消息换成多行，导致最新消息被固定高度文本框截断。`DebugOverlay` 现会测量文本框实际宽高，仅在 Overlay 中略去放不下的最旧消息；Unity Console 仍保留完整历史。
- `Assets/TextMesh Pro/chinese_chars.txt` 已按当前代码重建，补齐 Run 开始/结束、结算统计、地图生成失败、验证器错误、Seed 预览与批量验证文本，并保留按钮文字和无限弹药符号 `∞`；自动比对当前相关字符串字面量后没有缺失汉字。
- 关卡脚本已按 `Generation/Building/Rooms` 整理；Data 与 Prefab 也按用途建立子目录。提交前逐项比对了移动前后的文件内容与 `.meta`，原 GUID 均保留，场景和资源引用未因移动断开。
- 主场景从 `SampleScene.unity` 改名为 `Main_V1_5_2.unity`，沿用原场景 GUID，并已同步 `EditorBuildSettings`。场景中的 `LevelGraphDebugView` 正确引用当前 `LevelGenerationSettings.asset`。
- 步骤 2 完成后 `Assets/Scripts/` 共 97 个 `.cs` 文件；`dotnet build Assembly-CSharp.csproj --no-restore --no-incremental` 为 **0 error / 0 项目代码 warning**（40 条 warning 均来自 Unity Package）。下一步进入步骤 3，把纯数据图建造成实体房间和通道。
